---
phase: INV-02-macos-app-dmg-with-signing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md
autonomous: true
requirements:
  - INV-02-SC2   # cargo tauri build (no --target) produces .app and .dmg
  - INV-02-SC3-REINTERPRETED   # unsigned build + documented user workaround (replaces the deferred Dev ID + codesign --verify criterion per 02-CONTEXT.md)

must_haves:
  truths:
    - "`cargo tauri build` (no `--target` flag) succeeds on this macOS host with the post-Phase-1 cargo-xwin config still in place"
    - "A `.app` exists at `src-tauri/target/release/bundle/macos/Invisible.app`"
    - "A `.dmg` exists at `src-tauri/target/release/bundle/dmg/Invisible_0.1.0_<arch>.dmg`"
    - "`file src-tauri/target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri` reports a native Mach-O binary for the host's CPU arch (NOT a cross-compile artefact)"
    - "`codesign -dv` on the `.app` reports `Signature=adhoc` or `code object is not signed at all` (proves we did NOT accidentally pick up a Dev ID — required by 02-CONTEXT.md LOCKED decision)"
    - "`README.md` has a new `## Installation (macOS)` section containing both unblock paths: right-click -> Open AND the `xattr -dr com.apple.quarantine /Applications/Invisible.app` command, scoped to `/Applications/Invisible.app` (NOT a wildcard, per security T4)"
    - "`PHASE-VERIFICATION.md` records SHA256 + size + `file` output of both `.app/Contents/MacOS/invisible-tauri` and the `.dmg`, plus a criterion-by-criterion table"
  artifacts:
    - path: "README.md"
      provides: "User-facing Gatekeeper workaround section"
      contains: "## Installation (macOS)"
    - path: ".planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md"
      provides: "Phase 2 verification record with .app/.dmg evidence"
      contains: "SHA256"
    - path: "src-tauri/target/release/bundle/macos/Invisible.app"
      provides: "Native macOS app bundle (build artefact, gitignored — recorded by hash only)"
    - path: "src-tauri/target/release/bundle/dmg"
      provides: "Native macOS DMG installer (build artefact, gitignored — recorded by hash only)"
  key_links:
    - from: "README.md"
      to: "macOS Gatekeeper unblock workflow"
      via: "Installation (macOS) section with both right-click and xattr paths"
      pattern: "xattr -dr com.apple.quarantine /Applications/Invisible.app"
    - from: "PHASE-VERIFICATION.md"
      to: "native Mach-O proof"
      via: "`file` output verbatim, including arch field (arm64 or x86_64)"
      pattern: "Mach-O.*executable"
    - from: "cargo tauri build"
      to: "src-tauri/target/release/bundle/{macos,dmg}/"
      via: "default native target — NO --target flag (LOCKED in 02-CONTEXT.md)"
      pattern: "Built application at"
---

<objective>
Verify the native macOS `cargo tauri build` pipeline still works after Phase 1's cargo-xwin cross-compile setup, capture the `.app` and `.dmg` artefacts (SHA256, size, `file` output), add a `## Installation (macOS)` section to `README.md` documenting both Gatekeeper unblock paths, and seed the phase-level `PHASE-VERIFICATION.md` with the build evidence.

Purpose: Phase 1 added cargo-xwin machinery (`bundle.targets` includes `"nsis"`, `bundle.windows.wix` block, `bundle.publisher`) plus toolchain installs (cargo-xwin, x86_64-pc-windows-msvc target, brew nsis). None of those changes should touch the native macOS build path, but it has not been re-verified since the Phase 1 NSIS switch (commit `265d998`). This plan proves the native path is intact and produces the artefacts users will download in Phase 3.

Per 02-CONTEXT.md LOCKED decisions: the build is UNSIGNED. No `codesign` invocation, no notarisation, no `spctl` check, no `bundle.macOS.signingIdentity` config. The README workaround is the user-facing substitute for a Dev ID cert.

Output:
- README.md with a new `## Installation (macOS)` section (atomic commit).
- PHASE-VERIFICATION.md seeded with build evidence (atomic commit). Plan 02-02 appends an updater-keypair section to the same file.
- Build artefacts at `src-tauri/target/release/bundle/{macos,dmg}/` (gitignored — recorded by SHA256 only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/tauri-windows/ROADMAP.md
@.planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/02-CONTEXT.md
@src-tauri/tauri.conf.json

<interfaces>
<!-- Current state of src-tauri/tauri.conf.json (full file is short — included for clarity). -->
<!-- THIS PLAN DOES NOT MODIFY tauri.conf.json. Plan 02-02 adds the plugins.updater block. -->
<!-- This plan only needs to RUN `cargo tauri build` against the existing config and capture artefacts. -->

`bundle` block (Phase-1 final state):
```json
"bundle": {
  "active": true,
  "targets": ["app", "dmg", "nsis"],
  "icon": [...],
  "category": "DeveloperTool",
  "shortDescription": "Personal multi-agent dev cockpit",
  "longDescription": "...",
  "publisher": "The Profit Platform",
  "windows": { "wix": { "language": ["en-US"] } }
}
```

Expected native-build output paths (Tauri 2.11 conventions):
- App bundle: `src-tauri/target/release/bundle/macos/Invisible.app`
- App binary: `src-tauri/target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri`
  (the binary basename matches `[[bin]] name` in `src-tauri/Cargo.toml`, which is `invisible-tauri`)
- DMG: `src-tauri/target/release/bundle/dmg/Invisible_0.1.0_<arch>.dmg`
  where `<arch>` is `aarch64` on Apple Silicon, `x64` on Intel. This Mac is `arm64` per the Phase 1 verification doc header (`host: aarch64-apple-darwin`), so expect `Invisible_0.1.0_aarch64.dmg`.

The `[bundle.targets]` array still contains `"nsis"` — this is harmless on macOS because Tauri's bundler only honours target-OS-compatible entries on the current host. The native build will emit only `app` and `dmg`. The NSIS entry is dormant unless we re-run the cross-compile.

README.md current structure (line 1 starts with `# invisible`; line 9 has the Status line; line 13 has `## What it does`). The Phase-2 README edit inserts a brand-new `## Installation (macOS)` section. Place it AFTER the existing `## What it does` block and BEFORE `## Architecture` (around line 56–60 — verify with `grep -n '^##' README.md` before editing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-01-01: Native macOS build + artefact capture</name>
  <files>(produces build artefacts under src-tauri/target/release/bundle/ — gitignored)</files>
  <action>
Run the native `cargo tauri build` (no `--target` flag — LOCKED in 02-CONTEXT.md "Build pipeline") and verify the `.app` + `.dmg` land at expected paths. This is the first native build since Phase 1's NSIS switch; we are proving the cross-compile machinery did not regress native output.

1. From the worktree root (`/Users/ace/.invisible-ws/tauri-windows`):
   - `source "$HOME/.cargo/env"` — ensure `cargo` is on PATH.
   - `cd src-tauri`.

2. Run the native build. Use a 1800000 ms (30 min) timeout because the first native build after a long cross-compile session may need to recompile the dependency tree for the host target:
   ```
   cargo tauri build 2>&1 | tee /tmp/inv-02-01-build.log
   ```
   Do NOT pass `--target`, `--runner`, or `--bundles` — defaults produce both `.app` and `.dmg` on macOS.

   Expected end-of-log pattern (Tauri 2.11):
   ```
   Finished `release` profile [optimized] target(s) in ...
   Built application at: ...src-tauri/target/release/bundle/macos/Invisible.app
   Finished 2 bundles at:
       .../src-tauri/target/release/bundle/macos/Invisible.app
       .../src-tauri/target/release/bundle/dmg/Invisible_0.1.0_<arch>.dmg
   ```
   The `beforeBuildCommand` in `tauri.conf.json` will run `pnpm build` in `frontend-vite/` first — ensure pnpm/corepack is on PATH (it should be from Phase 1).

3. If the build fails, classify and react:
   - **`pnpm: command not found`** -> `corepack enable && corepack prepare pnpm@latest --activate`, then retry.
   - **frontend-vite/dist missing or stale** -> `cd /Users/ace/.invisible-ws/tauri-windows/frontend-vite && pnpm install --frozen-lockfile && pnpm build`, then retry the Tauri build.
   - **Any error referencing Windows / x86_64-pc-windows-msvc / makensis** -> STOP. That means `cargo tauri build` accidentally picked up cross-compile settings. Re-check that no `--target` flag is in scope and there is no `.cargo/config.toml` setting `target` to the windows triple.
   - **Anything else** -> STOP, capture last 50 lines of `/tmp/inv-02-01-build.log`, surface to the checker.

4. Confirm both artefacts exist at expected paths (use `arch=$(uname -m)` to derive the DMG arch suffix — on Apple Silicon `uname -m` reports `arm64` but Tauri writes `aarch64` in the filename, so check both forms with a glob):
   ```
   ls -la src-tauri/target/release/bundle/macos/Invisible.app
   ls -la src-tauri/target/release/bundle/dmg/Invisible_0.1.0_*.dmg
   ```

5. Capture metadata facts (Task 02-01-03 writes them into PHASE-VERIFICATION.md — collect into `/tmp/inv-02-01-facts.txt` so the next task can read them):
   ```
   APP=src-tauri/target/release/bundle/macos/Invisible.app
   BIN="$APP/Contents/MacOS/invisible-tauri"
   DMG=$(ls src-tauri/target/release/bundle/dmg/Invisible_0.1.0_*.dmg | head -1)

   {
     echo "=== APP ==="
     echo "PATH=$APP"
     echo "BIN=$BIN"
     echo "BIN_SHA256=$(shasum -a 256 "$BIN" | awk '{print $1}')"
     echo "BIN_SIZE_BYTES=$(stat -f %z "$BIN")"
     echo "BIN_FILE=$(file "$BIN" | sed 's|.*/||')"
     echo
     echo "=== DMG ==="
     echo "PATH=$DMG"
     echo "DMG_SHA256=$(shasum -a 256 "$DMG" | awk '{print $1}')"
     echo "DMG_SIZE_BYTES=$(stat -f %z "$DMG")"
     echo "DMG_FILE=$(file "$DMG" | sed 's|.*/||')"
     echo
     echo "=== UNSIGNED PROOF ==="
     echo "CODESIGN_OUT=$(codesign -dv "$APP" 2>&1 || true)"
     echo
     echo "=== TOOLCHAIN ==="
     echo "RUSTC=$(rustc --version)"
     echo "CARGO=$(cargo --version)"
     echo "TAURI_CLI=$(cargo tauri --version 2>&1 | head -1)"
   } > /tmp/inv-02-01-facts.txt

   cat /tmp/inv-02-01-facts.txt
   ```

   The `CODESIGN_OUT` line is the explicit unsigned-proof per 02-CONTEXT.md: it must say `code object is not signed at all` OR `Signature=adhoc` (Tauri 2.x sometimes ad-hoc signs to satisfy macOS linker, which is NOT a Dev ID signature and does NOT get Gatekeeper approval). It MUST NOT contain `Authority=Developer ID Application:`.

6. No commit — only gitignored build artefacts + `/tmp/inv-02-01-facts.txt`.
  </action>
  <verify>
    <automated>APP=src-tauri/target/release/bundle/macos/Invisible.app; BIN="$APP/Contents/MacOS/invisible-tauri"; test -d "$APP" && test -f "$BIN" && file "$BIN" | grep -qE "Mach-O.*executable" && ls src-tauri/target/release/bundle/dmg/Invisible_0.1.0_*.dmg >/dev/null 2>&1 && ! (codesign -dv "$APP" 2>&1 | grep -q "Authority=Developer ID") && test -s /tmp/inv-02-01-facts.txt && echo OK</automated>
  </verify>
  <done>`.app` directory exists at the expected path; the inner Mach-O binary exists and `file` reports `Mach-O ... executable`; a `.dmg` matching the `Invisible_0.1.0_*.dmg` glob exists in the dmg bundle directory; `codesign -dv` does NOT report a `Developer ID Application` authority (proves unsigned per 02-CONTEXT.md); `/tmp/inv-02-01-facts.txt` is non-empty. No git changes (`git status --porcelain` empty).</done>
</task>

<task type="auto">
  <name>Task 02-01-02: Add ## Installation (macOS) section to README.md</name>
  <files>README.md</files>
  <action>
Add a new `## Installation (macOS)` section to the root `README.md`. The section documents the two Gatekeeper unblock paths LOCKED in 02-CONTEXT.md "Gatekeeper workaround".

1. First, find the insertion point. Run:
   ```
   grep -n '^## ' README.md
   ```
   Expected output includes (approximately):
   - Line 1: `# invisible` (title, not a `##`)
   - Around line 13: `## What it does`
   - Around line 56–60: `## Architecture`

   The new section goes BETWEEN `## What it does` (and its sub-blocks, including the existing `### Tauri shell (Phase 2 — preview)` block on line ~39) and `## Architecture`. Place it as a sibling `##` heading immediately before `## Architecture`.

2. Use the Edit tool to insert the section. The exact content to insert (do not paraphrase, do not add extra sub-sections):

   ```markdown
   ## Installation (macOS)

   The Tauri shell ships **unsigned** for now (no Apple Developer ID — issue
   tracked in the workstream ROADMAP). macOS Gatekeeper will refuse to open
   the app on first launch with a "cannot be opened because the developer
   cannot be verified" dialog. Pick one of the two unblock paths below.

   **Path A — right-click open (recommended for non-technical users):**

   1. Drag `Invisible.app` from the `.dmg` into `/Applications`.
   2. In Finder, **right-click** `Invisible.app` -> **Open**.
   3. In the dialog that appears, click **Open** again.
   4. macOS remembers the decision — subsequent launches work normally.

   **Path B — terminal (faster if you already have Terminal open):**

   ```bash
   xattr -dr com.apple.quarantine /Applications/Invisible.app
   ```

   This strips the quarantine extended attribute that Gatekeeper added when
   the download landed. **Only run this on apps you downloaded yourself**
   from a source you trust — never apply it as a blanket "fix all the
   warnings" command.

   See [Apple's Gatekeeper docs](https://support.apple.com/guide/mac-help/open-a-mac-app-from-an-unknown-developer-mh40616/mac)
   for the official walkthrough.

   ```

   Two formatting constraints (security T4):
   - The `xattr` command MUST be scoped to `/Applications/Invisible.app` — NEVER write `xattr -dr com.apple.quarantine ~/Downloads` or any wildcard form.
   - The warning sentence ("Only run this on apps you downloaded yourself...") MUST be present.

3. Verify the edit landed cleanly. Run `grep -n '## Installation (macOS)' README.md` (should output exactly one line). Then `grep -n '^## ' README.md` and confirm the section order is: `## What it does` -> `## Installation (macOS)` -> `## Architecture`.

4. Confirm the README still renders as valid Markdown (no stray code-fence imbalance). The fastest check:
   ```
   awk '/^```/ {c++} END {print "fences=" c}' README.md
   ```
   `c` must be EVEN (every fence has a closing pair).

5. Commit atomically. Stage only README.md:
   - Message: `docs(tauri-windows): add macOS Gatekeeper unblock section to README`
   - Body: one-line rationale referencing 02-CONTEXT.md "Gatekeeper workaround" LOCKED decision.
  </action>
  <verify>
    <automated>grep -q '^## Installation (macOS)$' README.md && grep -q 'xattr -dr com.apple.quarantine /Applications/Invisible.app' README.md && grep -q 'right-click' README.md && grep -q 'apps you downloaded yourself' README.md && ! grep -E 'xattr -dr com.apple.quarantine \*|xattr -dr com.apple.quarantine ~' README.md && [ $(awk '/^```/ {c++} END {print c}' README.md) -eq $(awk '/^```/ {c++} END {print c}' README.md) ] && [ $(($(awk '/^```/ {c++} END {print c}' README.md) % 2)) -eq 0 ] && git log -1 --oneline -- README.md | grep -q 'docs(tauri-windows)' && echo OK</automated>
  </verify>
  <done>`README.md` contains an `## Installation (macOS)` section with both Path A (right-click -> Open) and Path B (`xattr -dr com.apple.quarantine /Applications/Invisible.app`); the xattr command is scoped to `/Applications/Invisible.app` (no wildcard / no `~/Downloads` form); the "apps you downloaded yourself" warning is present; code fences are balanced; commit landed on `ws/tauri-windows` with a `docs(tauri-windows):` Conventional Commit message.</done>
</task>

<task type="auto">
  <name>Task 02-01-03: Seed PHASE-VERIFICATION.md with build evidence</name>
  <files>.planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md</files>
  <action>
Create the phase-level verification document with all the build evidence captured in Task 02-01-01. This file is the durable record of "what shipped" for Phase 2. Plan 02-02 will APPEND a new section about the updater keypair — leave the file structure open to extension.

1. Read `/tmp/inv-02-01-facts.txt` (created by Task 02-01-01) — every `{...}` placeholder in the template below MUST be replaced with the actual value from that file. No `TBD`, no `TODO`, no placeholders left behind.

2. Create `.planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md` with this exact structure (mirroring Phase 1's format from `.planning/workstreams/tauri-windows/phases/INV-01-windows-msi-cross-compile/PHASE-VERIFICATION.md`):

   ```markdown
   ---
   phase: INV-02
   workstream: tauri-windows
   slug: macos-app-dmg-with-signing
   actual-target: unsigned-app-dmg
   verified-on: {YYYY-MM-DD of completion}
   host: {output of `uname -sr -m`}
   signing-decision: ship-unsigned-no-apple-dev-id
   signing-decision-source: .planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/02-CONTEXT.md
   app:
     path: src-tauri/target/release/bundle/macos/Invisible.app
     binary-path: src-tauri/target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri
     binary-sha256: {BIN_SHA256}
     binary-size-bytes: {BIN_SIZE_BYTES}
     binary-file-type: {BIN_FILE}
   dmg:
     path: {DMG_PATH from facts file — full src-tauri/.../Invisible_0.1.0_<arch>.dmg}
     sha256: {DMG_SHA256}
     size-bytes: {DMG_SIZE_BYTES}
     size-mb: {DMG_SIZE_BYTES / 1048576, 2 decimal places}
     file-type: {DMG_FILE}
   codesign-status: {one of: "unsigned (code object is not signed at all)" OR "adhoc (Signature=adhoc — Tauri linker ad-hoc, NOT a Dev ID signature)"}
   codesign-raw: |
     {CODESIGN_OUT verbatim, indented under the YAML block scalar}
   toolchain:
     rustc: {RUSTC}
     cargo: {CARGO}
     tauri-cli: {TAURI_CLI}
   ---

   # Phase 2 Verification — Native macOS .app + .dmg (UNSIGNED)

   ## Headline result

   `cargo tauri build` (no `--target` flag) produces a native macOS `.app`
   and `.dmg` from this host. The build is **unsigned** by design — see
   `02-CONTEXT.md` for the LOCKED decision to ship without an Apple
   Developer ID. Users unblock Gatekeeper via the workaround documented
   in the new `## Installation (macOS)` section of the root `README.md`.

   Plan 02-02 (next) adds the Tauri auto-updater public key + endpoint
   placeholder to `tauri.conf.json`. That plan APPENDS its own section to
   this file rather than overwriting; do not delete or restructure the
   sections below.

   ## ROADMAP success criteria walkthrough (reinterpreted per 02-CONTEXT.md)

   | # | Criterion (original) | Reinterpretation (per CONTEXT) | Status | Evidence |
   |---|----------------------|--------------------------------|--------|----------|
   | 1 | Developer ID Application certificate documented | DEFERRED — ship unsigned, README workaround documents the user-facing impact | DEFERRED-AS-LOCKED | `02-CONTEXT.md` "Apple Developer Program / Signing" LOCKED block; README `## Installation (macOS)` section (commit from Task 02-01-02) |
   | 2 | `cargo tauri build` (no target) produces `.app` and `.dmg` | (no change) | PASS | This task. `.app` and `.dmg` SHA256s + sizes recorded above in YAML frontmatter. |
   | 3 | `codesign --verify --verbose` returns 0; `spctl -a -t exec` reports accepted | DEFERRED — verify unsigned status instead | PASS-AS-REINTERPRETED | `codesign-status` field above proves no Dev ID Application authority is attached. |
   | 4 | Auto-updater public key + endpoint placeholders ready | (no change — handled in Plan 02-02) | PENDING — PLAN 02-02 | Plan 02-02 appends an "Updater keypair" section to this file. |

   ## Native-build proof (anti-regression vs Phase 1)

   Phase 1 added cargo-xwin machinery (`bundle.targets` includes `"nsis"`,
   `bundle.windows.wix` block, brew `nsis`, cargo `xwin` toolchain). This
   task proves none of those changes broke the native macOS build path.

   ### `.app` inner Mach-O binary

   ```
   $ file src-tauri/target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri
   {BIN_FILE}
   ```

   The output contains `Mach-O ... executable` — that is the native-build
   proof. A cross-compile artefact would say `PE32` (Windows) or `ELF`
   (Linux), not `Mach-O`.

   ### Codesign status (unsigned proof, per 02-CONTEXT.md LOCKED decision)

   ```
   $ codesign -dv src-tauri/target/release/bundle/macos/Invisible.app
   {CODESIGN_OUT}
   ```

   The output MUST NOT contain `Authority=Developer ID Application:`. If
   it does, an Apple Developer ID slipped into the build environment and
   the LOCKED decision was violated — re-run after `xcrun simctl spawn ...`
   etc. is cleaned up. Current output above is the proof that no Dev ID
   was attached.

   ## Build-log excerpt

   ```
   {last 20-30 lines of /tmp/inv-02-01-build.log — MUST include the
    "Built application at: ...Invisible.app" line and the "Finished N
    bundles at: ..." line for the dmg path}
   ```

   ## Re-verification recipe

   ```bash
   cd ~/.invisible-ws/tauri-windows
   source "$HOME/.cargo/env"
   cd src-tauri
   cargo tauri build
   ls -la target/release/bundle/macos/Invisible.app
   ls -la target/release/bundle/dmg/Invisible_0.1.0_*.dmg
   shasum -a 256 target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri
   shasum -a 256 target/release/bundle/dmg/Invisible_0.1.0_*.dmg
   file target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri
   codesign -dv target/release/bundle/macos/Invisible.app 2>&1 || true
   ```

   The SHA256s will vary across rebuilds (Tauri embeds build metadata).
   The structural assertions that matter are:
   - `Mach-O ... executable` in the `file` output for the inner binary.
   - No `Authority=Developer ID Application:` in `codesign -dv`.
   - DMG file size in the 10–25 MB range (compressed payload).

   ## Hand-off to Plan 02-02

   Plan 02-02 generates the Tauri auto-updater keypair and adds the
   `plugins.updater` block to `src-tauri/tauri.conf.json`. After that plan
   completes, the appended "Updater keypair" section in this file will
   document:
   - The public-key value (safe to commit) inline in `tauri.conf.json`.
   - The Infisical path of the private key (NOT the key value itself).
   - A re-run of `cargo tauri build` proving the new config did not break
     the native macOS build path verified in this plan.
   ```

3. Validate every `{...}` placeholder is gone:
   ```
   grep -E '\{[A-Z_0-9]+\}' .planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md && echo "PLACEHOLDERS REMAIN — FAIL" || echo "no placeholders"
   ```
   Output MUST be `no placeholders`.

4. Validate every criterion row is non-PENDING for criteria 1, 2, 3 (criterion 4 stays PENDING — Plan 02-02 fills it in):
   ```
   grep -E '^\| [123] \|' .planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md | grep -E 'PENDING|TBD|TODO' && echo "FAIL" || echo "OK"
   ```

5. Commit atomically. Stage only the PHASE-VERIFICATION.md file:
   - Message: `docs(INV-02): seed PHASE-VERIFICATION — unsigned .app/.dmg build`
   - Body: one line with the `.app` binary SHA256 and the `.dmg` SHA256 (grep-able).
  </action>
  <verify>
    <automated>F=.planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md; test -f "$F" && ! grep -qE '\{[A-Z_0-9]+\}' "$F" && grep -q '^# Phase 2 Verification' "$F" && grep -q 'Mach-O' "$F" && grep -q 'binary-sha256:' "$F" && grep -q 'codesign-status:' "$F" && ! grep -qE '^\| [123] \|.*\| PENDING\b' "$F" && grep -qE 'PENDING.*PLAN 02-02' "$F" && grep -Eq '[a-f0-9]{64}' "$F" && git log -1 --oneline -- "$F" | grep -q 'docs(INV-02)' && echo OK</automated>
  </verify>
  <done>`PHASE-VERIFICATION.md` exists with no `{...}` placeholders; contains the YAML frontmatter with `app.binary-sha256`, `dmg.sha256`, `codesign-status`, `toolchain`; the body mentions `Mach-O` (native proof); rows 1-3 of the success-criteria table are not `PENDING`; row 4 explicitly says PENDING-PLAN-02-02; at least one 64-hex-char SHA256 string is present; file is committed on `ws/tauri-windows` with a `docs(INV-02):` Conventional Commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local macOS host -> npm registry | `pnpm build` runs in `frontend-vite/` via Tauri's `beforeBuildCommand`. Owned by sibling workstreams; this phase does not modify `package.json` / `pnpm-lock.yaml`. |
| local macOS host -> README readers | The README workaround instructs end-users to run `xattr -dr com.apple.quarantine` against a specific path. Mis-scoped instructions become a foot-gun. |
| local filesystem -> repo working tree | Two files touched (`README.md`, `PHASE-VERIFICATION.md`). Build artefacts under `target/` are gitignored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INV-02-01-T4 | Tampering | README `xattr` instruction (security T4 from phase prompt) | mitigate | The `xattr -dr com.apple.quarantine` command in the README is scoped EXPLICITLY to `/Applications/Invisible.app` — Task 02-01-02 verify gate rejects any wildcard / `~/Downloads` form. The README also includes a warning sentence ("Only run this on apps you downloaded yourself") so users do not blanket-apply the workaround. |
| T-INV-02-01-SIGN-CLAIM | Spoofing | `bundle.publisher` claim ("The Profit Platform") on the `.app` / `.dmg` | accept | The `.app` and `.dmg` are unsigned by design per 02-CONTEXT.md LOCKED decision. Publisher is metadata only — no Apple Developer ID chain backs the claim. Any party could mint a similarly-named bundle; this is intrinsic to the ship-unsigned choice. README explicitly warns users that the app is unsigned. Phase 2.x (deferred) closes this if/when a $99 Dev ID is acquired. |
| T-INV-02-01-LOG | Information Disclosure | `/tmp/inv-02-01-build.log` and `/tmp/inv-02-01-facts.txt` | accept | Build logs and metadata are written to `/tmp/` for this session only. They do not contain secrets — no signing identity is used in this phase. macOS clears `/tmp/` on reboot. |
| T-INV-02-01-REGRESSION | Tampering | accidental Dev ID pickup from environment | mitigate | Task 02-01-01 verify gate runs `codesign -dv` and fails if `Authority=Developer ID Application:` appears in the output — the verify is the regression check. |
| T-INV-02-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new cargo/npm/pip installs introduced by this plan. `pnpm build` in `frontend-vite/` runs against a lockfile owned by sibling workstreams; their CI is the supply-chain authority. Phase-2 install surface is empty. |

T-INV-02-01-SIGN-CLAIM is the only "accept" with downstream impact — it is the consequence of the LOCKED ship-unsigned decision and is exactly what the README workaround addresses for users.
</threat_model>

<verification>
After all three tasks succeed, the plan passes if and only if:

1. `git log --oneline ws/tauri-shell..ws/tauri-windows -- README.md .planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md` shows exactly two new commits: one `docs(tauri-windows): ...Gatekeeper unblock...` and one `docs(INV-02): seed PHASE-VERIFICATION...`.
2. `test -d src-tauri/target/release/bundle/macos/Invisible.app && test -f src-tauri/target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri` succeeds (the artefact exists locally; it is gitignored).
3. `file src-tauri/target/release/bundle/macos/Invisible.app/Contents/MacOS/invisible-tauri | grep -q 'Mach-O.*executable'` succeeds.
4. `grep -q '^## Installation (macOS)$' README.md` succeeds.
5. `grep -qE '[a-f0-9]{64}' .planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/PHASE-VERIFICATION.md` succeeds.
6. `! codesign -dv src-tauri/target/release/bundle/macos/Invisible.app 2>&1 | grep -q 'Authority=Developer ID Application:'` succeeds (unsigned confirmed).

If any check fails, the plan does not advance to 02-02.
</verification>

<success_criteria>
This plan is complete when:
- The native macOS build pipeline produces `.app` + `.dmg` at expected paths (proves Phase 1's cargo-xwin setup did not regress native output).
- The `.app` inner binary is a native Mach-O executable for this host's arch (NOT a cross-compile artefact).
- The `.app` is verifiably UNSIGNED (no Dev ID Application authority in `codesign -dv` output).
- `README.md` has a new `## Installation (macOS)` section with both Path A (right-click -> Open) and Path B (`xattr -dr com.apple.quarantine /Applications/Invisible.app`), the xattr command is correctly scoped (no wildcard), and the user-warning sentence is present.
- `PHASE-VERIFICATION.md` records SHA256 + size + file-type + codesign-status for both `.app` binary and `.dmg`, with all `{...}` placeholders replaced.
- Both file edits are committed atomically with Conventional Commit messages on `ws/tauri-windows`.
- The PHASE-VERIFICATION.md file structure leaves room for Plan 02-02 to APPEND an "Updater keypair" section.
</success_criteria>

<output>
On completion, create `.planning/workstreams/tauri-windows/phases/INV-02-macos-app-dmg-with-signing/02-01-SUMMARY.md` summarising:
- The two commits landed (short SHAs + messages).
- The `.app` binary SHA256 + size + file type.
- The `.dmg` SHA256 + size + arch suffix.
- The codesign-status confirmation (unsigned or adhoc — NOT Dev ID).
- A one-line hand-off note for Plan 02-02 about appending the updater keypair section.
</output>
