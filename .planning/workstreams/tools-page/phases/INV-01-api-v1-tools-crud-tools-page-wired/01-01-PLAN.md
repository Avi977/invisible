---
phase: INV-01-api-v1-tools-crud-tools-page-wired
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/api/tools.py
  - lib/api/__init__.py
  - bin/invisible-dashboard
  - tests/test_api_tools.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01: workflows persist at config.home()/workflows/<project>.json, root derived from config.home() (never hardcoded)"
    - "D-02: writes are atomic and lock-free single-writer (tmpfile in same dir + os.replace), workflows/ dir created on first write with mkdir(parents=True, exist_ok=True)"
    - "D-03: project query param validated as strict slug ^[a-z0-9][a-z0-9_-]{0,63}$ BEFORE any path construction; empty / . / .. / slashes / encoded traversal rejected with HTTP 400; no value escapes workflows/"
    - "D-04: tools.py mirrors projects.py shape — transport-agnostic handle_*(handler) calling handler._send_json(obj, status); every handler wraps IO in try/except returning generic {\"error\":\"internal error\"} 500 that leaks no path or traceback"
    - "D-05: GET /api/v1/tools?project=<slug> returns {nodes, edges, updated_at}; missing file returns 200 {\"nodes\":[],\"edges\":[],\"updated_at\":null} (NOT 404); missing/invalid project returns 400"
    - "D-06: PUT /api/v1/tools?project=<slug> accepts JSON {nodes:[...],edges:[...]}, validates both are lists, stamps server-side updated_at via datetime.now(timezone.utc).isoformat(), writes atomically, returns {\"updated_at\": <value>}; malformed body 400; oversized body 413"
    - "D-07: DELETE /api/v1/tools?project=<slug> removes the workflow file returning 200 {\"deleted\":true}; missing file returns 404"
    - "D-08: module registered via `from . import tools` in lib/api/__init__.py bottom-block AND imported in bin/invisible-dashboard alongside other `from api import ...` lines"
    - "D-09: GET route added in do_GET as explicit return-terminated branch (if path == \"/api/v1/tools\": tools.handle_get(self); return), NOT routed through API_V1_ROUTES dispatch"
    - "D-10: new do_PUT and do_DELETE methods on DashboardHandler each call _auth_ok() first; do_PUT enforces Content-Length cap + JSON-parse→400; both dispatch /api/v1/tools, return 404 for unknown paths, guard with last-resort try/except→500 keeping traceback out of body"
    - "D-11: single-source CORS so every response carries exactly ONE Access-Control-Allow-Origin; the duplicate-ACAO bug (end_headers emits * while _send_json echoes Origin) eliminated; loopback-only echo (http://127.0.0.1:* / http://localhost:*) kept as single source"
    - "D-12: the two duplicate do_OPTIONS definitions collapsed into one advertising Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS; loopback-only origin policy and Access-Control-Max-Age preserved"
  artifacts:
    - path: "lib/api/tools.py"
      provides: "GET/PUT/DELETE handlers + atomic write helper + slug validator"
      contains: "def handle_get"
    - path: "lib/api/__init__.py"
      provides: "tools module registration"
      contains: "from . import tools"
    - path: "bin/invisible-dashboard"
      provides: "do_PUT, do_DELETE, GET /api/v1/tools route, single-source CORS, collapsed do_OPTIONS"
      contains: "def do_PUT"
    - path: "tests/test_api_tools.py"
      provides: "hermetic GET/PUT/DELETE + traversal + envelope tests"
      contains: "def test"
  key_links:
    - from: "bin/invisible-dashboard do_GET"
      to: "tools.handle_get"
      via: "explicit return-terminated branch"
      pattern: "tools\\.handle_get"
    - from: "bin/invisible-dashboard do_PUT"
      to: "tools.handle_put"
      via: "auth-gated + body-capped dispatch"
      pattern: "tools\\.handle_put"
    - from: "lib/api/tools.py"
      to: "config.home() / workflows"
      via: "path derivation"
      pattern: "config\\.home\\(\\)"
---

<objective>
Build the backend half of the Tools wiring: a new `lib/api/tools.py` CRUD module (GET/PUT/DELETE over a per-project JSON blob on disk), register it in the daemon, add `do_PUT`/`do_DELETE` HTTP methods, and fix the two confirmed CORS bugs (duplicate `Access-Control-Allow-Origin`, duplicate `do_OPTIONS`) so the frontend (Plan 01-02) can load and autosave cross-origin.

Purpose: The Tools canvas currently reads a static `TOOL_WORKFLOWS` mock. This plan delivers the real, traversal-safe, atomically-written persistence + the hardened HTTP surface that Plan 01-02 wires the UI to. Without the CORS fix the browser rejects every :8090 → :8765 fetch, so this MUST land before the frontend.

Output: `lib/api/tools.py` (new), one import line in `lib/api/__init__.py`, daemon changes in `bin/invisible-dashboard` (GET branch + `do_PUT` + `do_DELETE` + CORS single-source + collapsed `do_OPTIONS`), and a hermetic `tests/test_api_tools.py`.

Covers decisions D-01 through D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/tools-page/phases/INV-01-api-v1-tools-crud-tools-page-wired/01-CONTEXT.md
@.planning/workstreams/tools-page/phases/INV-01-api-v1-tools-crud-tools-page-wired/01-PATTERNS.md
@.planning/workstreams/tools-page/ROADMAP.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase — use directly, no exploration. -->

The transport-agnostic handler contract every tools.py handler must follow
(from lib/api/projects.py:301-321, handle_projects):
  - signature: handle_<verb>(handler: Any) -> None
  - body: do IO, then handler._send_json(obj, status=...)
  - wrap everything in try/except; on exception write "[api/tools] internal error: {type(exc).__name__}" to sys.stderr and handler._send_json({"error": "internal error"}, status=500)
  - NEVER put a path or traceback in the response body

The trust-boundary analog (from lib/api/projects.py:80-104, _safe_path):
  - validate-then-act posture, returns None on any escape
  - D-03 is STRICTER: regex-reject the attacker-controlled query param
    BEFORE any Path() construction (re.match(r"^[a-z0-9][a-z0-9_-]{0,63}$", project))

config root (from lib/api/projects.py:94 and lib/config.py:15-18):
  - config.home() resolves $INVISIBLE_HOME (defaults ~/.invisible), read at call time
  - workflows dir = config.home() / "workflows"; target = that / f"{project}.json"

Query-param parsing the daemon already uses (bin/invisible-dashboard:356-357, 372-373):
  - q = urllib.parse.parse_qs(urllib.parse.urlparse(handler.path).query)
  - project = q.get("project", [None])[0]

do_POST template to mirror for do_PUT/do_DELETE (bin/invisible-dashboard:433-492):
  - _auth_ok() first → 401 on fail (so --no-auth short-circuits)
  - Content-Length read with try/except ValueError → 0; cap at self._MAX_POST_BYTES (32_768) → 413 {"error":"message_too_large"}
  - json.loads(raw) with except (JSONDecodeError, UnicodeDecodeError) → 400 {"error":"bad_request"}
  - dispatch by path, else self._send_text("not found\n", 404)
  - last-resort except Exception → traceback.print_exc() (stderr) + _send_json({"error":"server_error"}, 500)

CORS bug sites to fix (bin/invisible-dashboard):
  - end_headers() at 228-232 unconditionally sends Access-Control-Allow-Origin: * (the duplicate source)
  - _send_json() at 289-300 echoes loopback Origin (the intended single source) + Vary: Origin
  - do_OPTIONS #1 at 234-252 (Max-Age for /api/v1/tree/) and do_OPTIONS #2 at 409-427 (loopback-gated, advertises only "GET, OPTIONS"); Python keeps the LAST def so #2 wins

Test harness shape (from tests/test_api_projects.py:24-27, 84-87):
  - put repo lib/ on sys.path: LIB = Path(__file__).resolve().parent.parent / "lib"; sys.path.insert(0, str(LIB))
  - hermetic fixture: monkeypatch.setenv("INVISIBLE_HOME", str(tmp_path)); config.home() reads it at call time, so no module reload is needed for tools.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/api/tools.py CRUD module (slug validator + atomic write + GET/PUT/DELETE handlers)</name>
  <files>lib/api/tools.py, tests/test_api_tools.py</files>
  <read_first>
    - lib/api/projects.py:1-35 (module docstring + import shape: `from __future__ import annotations`, `import config`)
    - lib/api/projects.py:80-104 (_safe_path — the validate-then-reject trust-boundary analog for D-03)
    - lib/api/projects.py:218-220 (_id_for — slug shape reference; D-03 uses the stricter regex-reject instead)
    - lib/api/projects.py:301-321 (handle_projects — the exact handle_*(handler) → _send_json contract + generic-500/no-leak pattern for D-04)
    - lib/config.py:15-18 (config.home() reads $INVISIBLE_HOME at call time)
    - tests/test_api_projects.py:1-90 (hermetic test harness: sys.path setup, monkeypatch INVISIBLE_HOME → tmp_path, fixture shape)
    - 01-PATTERNS.md lines 26-105 (the lib/api/tools.py pattern assignment with the verbatim _safe_path, handle_projects, and _write_atomic excerpts)
  </read_first>
  <behavior>
    Write tests/test_api_tools.py FIRST (RED) using a fake handler that records (obj, status) from _send_json. Tests assert:
    - GET on a never-saved project → status 200, body {"nodes":[],"edges":[],"updated_at":null} (D-05)
    - PUT {"nodes":[{"id":"a","kind":"Claude"}],"edges":[]} → status 200, body has "updated_at" as a non-null ISO-8601 string; the file config.home()/workflows/<project>.json now exists (D-06, D-02)
    - GET after that PUT → status 200, body nodes/edges round-trip exactly, updated_at matches what PUT returned (D-05/D-06)
    - PUT with nodes not a list (e.g. {"nodes":"x","edges":[]}) → status 400, no file written (D-06)
    - DELETE on an existing file → status 200, body {"deleted":true}, file gone (D-07)
    - DELETE on a missing file → status 404 (D-07)
    - Slug validation (D-03): project="" , project=".." , project="../etc" , project="a/b" , project="A" (uppercase out of charset) each → status 400 and NO file created anywhere under workflows/ (assert the workflows dir either absent or unchanged)
    - Atomicity smell-test (D-02): after a PUT, no leftover *.tmp file remains in the workflows dir
    All tests monkeypatch INVISIBLE_HOME to tmp_path so nothing touches the real ~/.invisible.
  </behavior>
  <action>
    Create lib/api/tools.py mirroring projects.py's module shape: `from __future__ import annotations`; stdlib imports `json, os, re, sys, tempfile`; `from datetime import datetime, timezone`; `from pathlib import Path`; `from typing import Any`; `import config`. Add a "Data flow / Security notes" docstring in the projects.py style stating the slug-reject trust boundary, the atomic-write model, and the no-leak 500 posture.

    Implement a slug validator (D-03) that applies re.match with the intent of the pattern ^[a-z0-9][a-z0-9_-]{0,63}$ (lowercase alnum start, then up to 63 of lowercase-alnum/underscore/hyphen) and returns False/None for anything else — this MUST run BEFORE any Path is constructed, so empty, ".", "..", strings containing "/" or "%" or uppercase all fail. Read the project param off handler.path via urllib.parse.parse_qs(urllib.parse.urlparse(handler.path).query).get("project", [None])[0]. On invalid slug call handler._send_json({"error":"bad_request"}, status=400) and return.

    Implement _write_atomic(target, obj) using the stdlib tmpfile-in-same-dir pattern: target.parent.mkdir(parents=True, exist_ok=True) (D-02 first-write mkdir), tempfile.mkstemp(dir=str(target.parent), suffix=".tmp"), write JSON via os.fdopen with json.dump(obj, f, indent=2) then f.flush() + os.fsync(f.fileno()), then os.replace(tmp, target) (atomic same-filesystem rename); on any exception unlink the tmp file (best-effort) and re-raise. Derive the target as config.home() / "workflows" / f"{project}.json" — NEVER hardcode ~/.invisible (D-01).

    Expose three handlers (D-04): handle_get(handler), handle_put(handler), handle_delete(handler). Each: validate the slug first (400 on fail), then in a try/except that on Exception writes only f"[api/tools] internal error: {type(exc).__name__}" to sys.stderr and calls handler._send_json({"error":"internal error"}, status=500). handle_get: if the target file exists, read+json.load it and _send_json({"nodes":..., "edges":..., "updated_at":...}); if missing, _send_json({"nodes":[],"edges":[],"updated_at":null}, status=200) — NOT 404 (D-05). handle_put: read the parsed body — the daemon already parsed JSON, so accept it via a handler attribute or re-read; validate isinstance(nodes, list) and isinstance(edges, list) else _send_json({"error":"bad_request"}, 400); stamp updated_at = datetime.now(timezone.utc).isoformat(); _write_atomic the {nodes, edges, updated_at} dict; _send_json({"updated_at": updated_at}) (D-06, keep envelope minimal per Claude's-discretion). handle_delete: target.unlink(); on FileNotFoundError _send_json({"error":"not found"}, 404); on success _send_json({"deleted": True}) (D-07).

    Decide the body hand-off for handle_put: the daemon's do_PUT (Task 2) will parse JSON and stash it on the handler (e.g. handler._json_body); handle_put reads that attribute (default {}). Document this contract in a one-line comment so Task 2 sets the same attribute name.

    Write tests/test_api_tools.py per <behavior> with a FakeHandler exposing path, _json_body, and a _send_json(obj, status=200) that records the last (obj, status). Mirror the sys.path + INVISIBLE_HOME monkeypatch setup from tests/test_api_projects.py:24-27,84-87. Run the suite, watch it fail (RED), implement, then make it pass (GREEN).
  </action>
  <acceptance_criteria>
    <automated>cd /Users/ace/.invisible-ws/tools-page && python3 -m pytest tests/test_api_tools.py -x -q</automated>
    <source>lib/api/tools.py defines `def handle_get`, `def handle_put`, `def handle_delete`, and an atomic-write helper using `os.replace`; verify: grep -E "def handle_get|def handle_put|def handle_delete|os.replace" lib/api/tools.py</source>
    <source>lib/api/tools.py derives its root from config.home(), never a literal path; verify NO hardcoded home: grep -c "~/.invisible" lib/api/tools.py returns 0, and grep -c "config.home()" lib/api/tools.py is >= 1</source>
    <source>slug validation precedes path construction: grep -n "re\\." lib/api/tools.py shows a regex gate; the test suite's traversal cases (project=../etc, project=a/b, project="") all assert status 400 with no file written</source>
    <behavior>after the test run, no *.tmp file remains in any tmp workflows dir (asserted inside the suite per D-02)</behavior>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/tools-page && python3 -m pytest tests/test_api_tools.py -x -q</automated>
  </verify>
  <done>tests/test_api_tools.py passes; tools.py exposes handle_get/put/delete with regex slug-reject (D-03) before any path build, atomic os.replace write + first-write mkdir (D-02), config.home()-derived path (D-01), GET empty-200 for missing file (D-05), PUT list-validate + ISO-8601 updated_at (D-06), DELETE 200/404 (D-07), and generic no-leak 500 (D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Register tools module + add GET branch, do_PUT, do_DELETE to the daemon</name>
  <files>lib/api/__init__.py, bin/invisible-dashboard</files>
  <read_first>
    - lib/api/__init__.py:36-39 (the bottom-block `from . import chat / tree_*` imports — where `from . import tools` belongs, D-08)
    - lib/api/__init__.py:22-25 (the ROUTES dict — D-09 says do NOT add /api/v1/tools here; route it explicitly in do_GET instead because the registry dispatch is missing its return)
    - bin/invisible-dashboard:62-64 (the `from api import ...` import block — where to import tools, D-08)
    - bin/invisible-dashboard:386-390 (the /api/v1/tree/repo GET branch — the correct return-terminated route pattern for D-09)
    - bin/invisible-dashboard:364-365 (the API_V1_ROUTES dispatch that is MISSING its return — the bug D-09 tells you to avoid)
    - bin/invisible-dashboard:407 (the trailing `self._send_text("not found\n", 404)` in do_GET — insert the new branch BEFORE this)
    - bin/invisible-dashboard:433-492 (do_POST — the full auth-gate + body-cap + JSON-parse→400 + dispatch + last-resort 500 template to copy for do_PUT/do_DELETE, D-10)
    - bin/invisible-dashboard:266-271 (_auth_ok — called first in both new methods)
    - bin/invisible-dashboard:431 (_MAX_POST_BYTES = 32_768 — reuse for the do_PUT body cap)
    - 01-PATTERNS.md lines 109-190 (the __init__.py + daemon wiring pattern assignment with verbatim excerpts)
  </read_first>
  <action>
    In lib/api/__init__.py append exactly one line inside the existing bottom import block (after line 39): `from . import tools       # noqa: F401  (tools: GET/PUT/DELETE /api/v1/tools)` (D-08). Do NOT add /api/v1/tools to the ROUTES dict (D-09). Adding "tools" to __all__ is optional and harmless.

    In bin/invisible-dashboard, add to the import block near lines 62-64: `from api import tools  # noqa: E402  — GET/PUT/DELETE /api/v1/tools` (D-08).

    GET route (D-09): in do_GET, immediately BEFORE the trailing `self._send_text("not found\n", 404)` at line 407, add a return-terminated branch modeled on the /api/v1/tree/repo branch: `if path == "/api/v1/tools": tools.handle_get(self); return`. Do NOT route through API_V1_ROUTES.

    do_PUT (D-10): add a new `def do_PUT(self) -> None:` method to DashboardHandler that copies do_POST's structure verbatim — compute path; call self._auth_ok() first and on failure write the same stderr auth-fail line and self._send_text("unauthorized\n", 401); return; read Content-Length with the same try/except ValueError → 0 and the same `if length > self._MAX_POST_BYTES` → 413 {"error":"message_too_large"} guard; read the body and json.loads it with the same except (JSONDecodeError, UnicodeDecodeError) → 400 {"error":"bad_request"} guard; stash the parsed payload on the handler as `self._json_body = payload` (the attribute name tools.handle_put reads — keep it identical to Task 1's contract); then in a try/except dispatch `if path == "/api/v1/tools": tools.handle_put(self); return` else `self._send_text("not found\n", 404)`; last-resort `except Exception:` → traceback.print_exc() + self._send_json({"error":"server_error"}, 500).

    do_DELETE (D-10): add a new `def do_DELETE(self) -> None:` method — compute path; _auth_ok() first → 401 on fail (same stderr line + unauthorized text); NO body read; in a try/except dispatch `if path == "/api/v1/tools": tools.handle_delete(self); return` else 404; last-resort except → 500 with traceback to stderr only. Also correct the now-false module docstring at bin/invisible-dashboard:24-30 ("What it does NOT do: Writes. Everything is GET. No POST...") to acknowledge the new PUT/DELETE write endpoints on /api/v1/tools — a one-line factual fix, do not expand scope.

    Keep every handled branch return-terminated and keep all tracebacks in stderr only (no path/traceback in any response body).
  </action>
  <acceptance_criteria>
    <automated>cd /Users/ace/.invisible-ws/tools-page && python3 -c "import sys; sys.path.insert(0,'lib'); import api; assert hasattr(api,'tools'); from api import tools; assert hasattr(tools,'handle_get') and hasattr(tools,'handle_put') and hasattr(tools,'handle_delete'); print('import OK')"</automated>
    <automated>cd /Users/ace/.invisible-ws/tools-page && python3 -c "import ast,sys; t=ast.parse(open('bin/invisible-dashboard').read()); m=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)]; assert 'do_PUT' in m and 'do_DELETE' in m, m; print('methods OK')"</automated>
    <source>exactly one import added to __init__: grep -c "from . import tools" lib/api/__init__.py returns 1</source>
    <source>GET routed explicitly, not via registry: grep -n "tools.handle_get" bin/invisible-dashboard shows a line, and that branch ends in `return` (inspect the two lines after the match)</source>
    <source>do_PUT stashes the body under the same attribute tools.handle_put reads: grep -n "_json_body" bin/invisible-dashboard and grep -n "_json_body" lib/api/tools.py both match the same identifier</source>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/tools-page && python3 -c "import sys; sys.path.insert(0,'lib'); import api; from api import tools; print('ok' if all(hasattr(tools,h) for h in ('handle_get','handle_put','handle_delete')) else 'FAIL')" && python3 -c "import ast; m=[n.name for n in ast.walk(ast.parse(open('bin/invisible-dashboard').read())) if isinstance(n,ast.FunctionDef)]; print('ok' if {'do_PUT','do_DELETE'} <= set(m) else 'FAIL')"</automated>
  </verify>
  <done>`from . import tools` registered (D-08); GET /api/v1/tools routed as an explicit return-terminated branch, not via the returnless registry (D-09); do_PUT and do_DELETE exist, auth-gate first, do_PUT enforces the 413 body cap + 400 JSON-parse and stashes _json_body for handle_put, both dispatch /api/v1/tools and 404 otherwise with last-resort 500 (D-10); the daemon imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Fix CORS — single-source Access-Control-Allow-Origin + collapse duplicate do_OPTIONS</name>
  <files>bin/invisible-dashboard</files>
  <read_first>
    - bin/invisible-dashboard:228-232 (end_headers override — emits the duplicate `Access-Control-Allow-Origin: *`, the bug to remove for D-11)
    - bin/invisible-dashboard:283-301 (_send_json — already echoes the loopback Origin + Vary: Origin; this is the intended SINGLE source to keep, D-11)
    - bin/invisible-dashboard:274-281 (_send_html) and 303-309 (_send_text) — confirm whether they rely on end_headers() for CORS (they call self.end_headers()); the single-source set must still cover any response that needs CORS
    - bin/invisible-dashboard:234-252 (do_OPTIONS #1 — Max-Age for /api/v1/tree/; the one to DELETE, D-12)
    - bin/invisible-dashboard:409-427 (do_OPTIONS #2 — loopback-gated, currently the winner, advertises only "GET, OPTIONS"; the one to KEEP and edit, D-12)
    - 01-PATTERNS.md lines 192-241 (the CORS single-source + do_OPTIONS-collapse pattern assignment with verbatim excerpts and the "Do it like this" guidance)
    - 01-CONTEXT.md lines 105-107 (the live-daemon evidence: two ACAO headers observed, OPTIONS advertised only GET/POST, second do_OPTIONS wins)
  </read_first>
  <action>
    Single-source the CORS origin (D-11): remove the `self.send_header("Access-Control-Allow-Origin", "*")` line from the end_headers() override (lines 228-232). To avoid any double-emission and keep exactly ONE Access-Control-Allow-Origin per response, drop the ACAO/ACAM/ACAH triplet from end_headers() entirely and instead emit the loopback-gated CORS set once in the response helpers: _send_json already does this (lines 289-300) — leave it as the single source; verify _send_text and _send_html also carry the loopback-gated Access-Control-Allow-Origin echo + Vary: Origin when an Origin header is present (add the same loopback-gated echo block to _send_text/_send_html if removing it from end_headers() would otherwise leave them with no ACAO). Net invariant: every response that sets CORS carries exactly ONE Access-Control-Allow-Origin, and only for Origins starting http://127.0.0.1: or http://localhost: (never a bare `*`). Update the now-stale CORS comment block at lines 222-232 to describe the loopback-only single-source policy (the old comment claims `*` is safe — it is being removed).

    Collapse the two do_OPTIONS (D-12): delete the FIRST do_OPTIONS definition (lines 234-252). Keep the SECOND (lines 409-427) and edit its Access-Control-Allow-Methods header value from "GET, OPTIONS" to "GET, POST, PUT, DELETE, OPTIONS" so the browser preflight permits the autosave PUT and the DELETE. Preserve its loopback-only origin gate, the Vary: Origin header, the Access-Control-Allow-Headers ("Authorization, Content-Type"), and the Access-Control-Max-Age ("600"). Since the deleted def #1 was the only place that special-cased /api/v1/tree/ for Max-Age, confirm the surviving def already emits Max-Age unconditionally for loopback origins (it does, line 425) so tree preflights keep their cache.

    Do not introduce Access-Control-Allow-Credentials anywhere (the loopback echo without credentials is the security posture). The `--no-auth` pin to 127.0.0.1 plus the loopback-only origin gate is what keeps the API closed to other web origins.
  </action>
  <acceptance_criteria>
    <automated>cd /Users/ace/.invisible-ws/tools-page && python3 -c "import ast; src=open('bin/invisible-dashboard').read(); n=sum(1 for d in ast.walk(ast.parse(src)) if isinstance(d,ast.FunctionDef) and d.name=='do_OPTIONS'); print('do_OPTIONS defs:',n); assert n==1, 'expected exactly one do_OPTIONS'"</automated>
    <source>end_headers no longer emits a bare wildcard ACAO: grep -n 'Access-Control-Allow-Origin", "\\*"' bin/invisible-dashboard returns nothing</source>
    <source>the surviving do_OPTIONS advertises all four+OPTIONS methods: grep -n "GET, POST, PUT, DELETE, OPTIONS" bin/invisible-dashboard matches</source>
    <behavior>live-daemon check — start the daemon on a scratch port with --no-auth and assert exactly ONE Access-Control-Allow-Origin on a GET and that OPTIONS advertises PUT/DELETE (commands below)</behavior>
    <automated>cd /Users/ace/.invisible-ws/tools-page && (INVISIBLE_HOME="$(mktemp -d)" ./bin/invisible-dashboard --no-auth --port 8799 >/tmp/inv-dash-8799.log 2>&1 &) ; sleep 1; ACAO=$(curl -s -D - -o /dev/null -H "Origin: http://127.0.0.1:8090" "http://127.0.0.1:8799/api/v1/tools?project=jobslayer" | grep -ci "^access-control-allow-origin:"); OPTM=$(curl -s -D - -o /dev/null -X OPTIONS -H "Origin: http://127.0.0.1:8090" "http://127.0.0.1:8799/api/v1/tools?project=jobslayer" | grep -i "access-control-allow-methods" | grep -c "PUT"); pkill -f "invisible-dashboard --no-auth --port 8799"; echo "ACAO_count=$ACAO PUT_advertised=$OPTM"; test "$ACAO" = "1" -a "$OPTM" = "1"</automated>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/tools-page && python3 -c "import ast; print('ok' if sum(1 for d in ast.walk(ast.parse(open('bin/invisible-dashboard').read())) if isinstance(d,ast.FunctionDef) and d.name=='do_OPTIONS')==1 else 'FAIL')"</automated>
  </verify>
  <done>Exactly one do_OPTIONS remains, advertising GET, POST, PUT, DELETE, OPTIONS with the loopback-only origin gate + Vary + Max-Age preserved (D-12); end_headers no longer emits a wildcard ACAO; a live GET returns exactly ONE loopback-echoed Access-Control-Allow-Origin and the OPTIONS preflight permits PUT/DELETE (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (:8090) → dashboard daemon (:8765) | cross-origin fetch; the `project` query param and the PUT body are untrusted input crossing into filesystem operations |
| HTTP client → do_PUT/do_DELETE | new write methods; first authenticated surface that mutates disk state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INV01-01 | Tampering (path traversal, HIGH) | `project` query param → `config.home()/workflows/<project>.json` | mitigate | D-03 strict slug regex `^[a-z0-9][a-z0-9_-]{0,63}$` applied BEFORE any Path construction; reject `""`/`.`/`..`/slashes/encoded-traversal/uppercase with 400. Acceptance test asserts `project=../etc`, `project=a/b`, `project=""` return 400 and write nothing outside `workflows/`. |
| T-INV01-02 | Denial of Service (unbounded body, MEDIUM) | do_PUT request body | mitigate | Reuse do_POST's `_MAX_POST_BYTES = 32_768` cap → 413 before reading/parsing an oversized body (D-10). |
| T-INV01-03 | Elevation of Privilege (auth bypass, HIGH) | new do_PUT / do_DELETE | mitigate | Both methods call `_auth_ok()` as the first action (D-10); `--no-auth` short-circuits exactly like do_POST, otherwise bearer-token auth is enforced. |
| T-INV01-04 | Information Disclosure (CORS over-permissiveness, HIGH) | end_headers / _send_json / do_OPTIONS | mitigate | D-11/D-12 single-source the loopback-only origin echo (`http://127.0.0.1:*` / `http://localhost:*`); remove the bare `*` ACAO; never set Access-Control-Allow-Credentials. Acceptance test asserts exactly ONE ACAO header and only for loopback origins. |
| T-INV01-05 | Information Disclosure (path/traceback leak, MEDIUM) | every tools.py handler + do_PUT/do_DELETE last-resort guard | mitigate | D-04 generic `{"error":"internal error"}` 500; log only `type(exc).__name__` to stderr; daemon last-resort `traceback.print_exc()` goes to stderr only, response body is `{"error":"server_error"}` (D-10). |
| T-INV01-06 | Tampering (concurrent partial write) | atomic write to `<project>.json` | accept | Lock-free single-writer per project is the chosen model (CONTEXT Deferred Ideas); `os.replace` makes each write atomic so a reader never sees a torn file. Multi-writer concurrency explicitly out of scope. |
| T-INV01-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | No new third-party packages introduced — implementation is pure stdlib (`json/os/re/tempfile/datetime/urllib`). No package-legitimacy checkpoint required for this plan. |

No HIGH threat is left undispositioned: T-INV01-01, -03, -04 are all mitigated with specific, testable controls. Block-on-HIGH is satisfied.
</threat_model>

<verification>
Run the full acceptance set after all three tasks:

```bash
cd /Users/ace/.invisible-ws/tools-page

# Unit tests (Task 1)
python3 -m pytest tests/test_api_tools.py -x -q

# Daemon imports + new methods present (Task 2)
python3 -c "import sys; sys.path.insert(0,'lib'); from api import tools; assert all(hasattr(tools,h) for h in ('handle_get','handle_put','handle_delete'))"
python3 -c "import ast; m={n.name for n in ast.walk(ast.parse(open('bin/invisible-dashboard').read())) if isinstance(n,ast.FunctionDef)}; assert {'do_PUT','do_DELETE'} <= m and list(n.name for n in ast.walk(ast.parse(open('bin/invisible-dashboard').read())) if getattr(n,'name','')=='do_OPTIONS').count('do_OPTIONS')==1"

# Live end-to-end against a scratch daemon (Tasks 2+3) — mirrors ROADMAP "Verify locally"
INVISIBLE_HOME="$(mktemp -d)" ./bin/invisible-dashboard --no-auth --port 8799 >/tmp/inv-dash-8799.log 2>&1 &
sleep 1
PROJECT=jobslayer
curl -s -X PUT -H 'Content-Type: application/json' "http://127.0.0.1:8799/api/v1/tools?project=$PROJECT" -d '{"nodes":[{"id":"a","kind":"Claude"}],"edges":[]}' | python3 -m json.tool   # expect {"updated_at": "...ISO..."}
curl -s "http://127.0.0.1:8799/api/v1/tools?project=$PROJECT" | python3 -m json.tool                                                                                # expect nodes/edges/updated_at
curl -s -o /dev/null -w '%{http_code}\n' "http://127.0.0.1:8799/api/v1/tools?project=missingproj"                                                                     # expect 200 (empty workflow)
curl -s -o /dev/null -w '%{http_code}\n' "http://127.0.0.1:8799/api/v1/tools?project=../etc"                                                                          # expect 400 (traversal blocked)
curl -s -X DELETE "http://127.0.0.1:8799/api/v1/tools?project=$PROJECT" | python3 -m json.tool                                                                        # expect {"deleted": true}
curl -s -o /dev/null -w '%{http_code}\n' -X DELETE "http://127.0.0.1:8799/api/v1/tools?project=$PROJECT"                                                               # expect 404 (now missing)
# CORS: exactly one ACAO, OPTIONS advertises PUT/DELETE
curl -s -D - -o /dev/null -H "Origin: http://127.0.0.1:8090" "http://127.0.0.1:8799/api/v1/tools?project=$PROJECT" | grep -ci "^access-control-allow-origin:"          # expect 1
curl -s -D - -o /dev/null -X OPTIONS -H "Origin: http://127.0.0.1:8090" "http://127.0.0.1:8799/api/v1/tools?project=$PROJECT" | grep -i "access-control-allow-methods" # expect GET, POST, PUT, DELETE, OPTIONS
pkill -f "invisible-dashboard --no-auth --port 8799"
```
</verification>

<success_criteria>
- ROADMAP Success Criterion 1: GET returns `{nodes, edges, updated_at}` (and 200 empty for never-saved) — D-05.
- ROADMAP Success Criterion 2: PUT writes atomically (tmpfile + os.replace) and returns the new `updated_at` — D-02, D-06.
- ROADMAP Success Criterion 3: DELETE removes the file (200) or 404 if missing — D-07.
- Traversal (`project=../etc`) returns 400 and writes nothing outside `workflows/` — D-03.
- The frontend cross-origin fetch (:8090 → :8765) is unblocked: exactly one loopback Access-Control-Allow-Origin and OPTIONS advertising PUT/DELETE — D-11, D-12.
- All of D-01..D-12 implemented with the specific controls above; no path/traceback leaks in any error body (D-04).
</success_criteria>

<output>
Create `.planning/workstreams/tools-page/phases/INV-01-api-v1-tools-crud-tools-page-wired/01-01-SUMMARY.md` when done.
</output>
