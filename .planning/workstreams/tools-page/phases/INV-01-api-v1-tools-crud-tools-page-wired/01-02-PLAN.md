---
phase: INV-01-api-v1-tools-crud-tools-page-wired
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/pages/tools.jsx
  - frontend/data.jsx
  - .gitignore
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-13: tools.jsx loads the workflow via fetch(API_BASE + \"/api/v1/tools?project=\" + projId) on project switch (mirroring folders.jsx API_BASE/headers/loading/error), seeding the canvas from fetched {nodes,edges} instead of TOOL_WORKFLOWS[projId]"
    - "D-14: autosave — on every node add / drag-end / wire add or remove, debounce 1s then PUT {nodes,edges}; cancel or flush the pending timer on project switch so a save for project A can never land on project B; footer renders saving…, saved Ns ago, and an error state"
    - "D-15: TOOL_WORKFLOWS mock removed entirely from frontend/data.jsx (delete the const ~line 171 AND drop TOOL_WORKFLOWS from the Object.assign(window,...) line 461); ProjectPicker preview updated to no longer read the deleted mock (neutral label or fetched counts)"
    - "D-16: workflows/ added to .gitignore (per-machine state the daemon creates under the repo root when run with INVISIBLE_HOME=$(pwd) inside a worktree)"
  artifacts:
    - path: "frontend/pages/tools.jsx"
      provides: "fetch-on-switch load, 1s-debounced autosave PUT with flush-on-switch, status footer, mock-free ProjectPicker + Tools body"
      contains: "/api/v1/tools"
    - path: "frontend/data.jsx"
      provides: "TOOL_WORKFLOWS removed (const + window assignment)"
    - path: ".gitignore"
      provides: "workflows/ ignore entry"
      contains: "workflows/"
  key_links:
    - from: "frontend/pages/tools.jsx"
      to: "/api/v1/tools (GET)"
      via: "fetch in a project-keyed useEffect with cancellation"
      pattern: "fetch\\(API_BASE \\+ \"/api/v1/tools"
    - from: "frontend/pages/tools.jsx"
      to: "/api/v1/tools (PUT)"
      via: "debounced autosave with flush-on-switch cleanup"
      pattern: "method:\\s*\"PUT\""
---

<objective>
Wire the existing Tools canvas to the real backend from Plan 01-01: load each project's workflow on switch, autosave changes with a 1s debounce, render a saving/saved footer, and delete the static `TOOL_WORKFLOWS` mock (fixing the two call sites that would otherwise throw a ReferenceError). Verify end-to-end in a real browser.

Purpose: This makes the Tools page read and write real per-project workflows — the user-visible payoff of the phase. It depends on 01-01 because its acceptance (cross-origin load with no CORS error, debounced PUT, traversal-safe API) requires the live backend route and the CORS single-source fix that 01-01 ships.

Output: changes to `frontend/pages/tools.jsx` (load + autosave + footer + mock-free render), `frontend/data.jsx` (mock removal), `.gitignore` (`workflows/`), and an observed real-browser verification run.

Covers decisions D-13 through D-16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/tools-page/phases/INV-01-api-v1-tools-crud-tools-page-wired/01-CONTEXT.md
@.planning/workstreams/tools-page/phases/INV-01-api-v1-tools-crud-tools-page-wired/01-PATTERNS.md
@.planning/workstreams/tools-page/ROADMAP.md
@.planning/workstreams/tools-page/phases/INV-01-api-v1-tools-crud-tools-page-wired/01-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase — use directly, no exploration. -->

The backend contract this plan consumes (delivered by Plan 01-01, see its SUMMARY):
  - GET  /api/v1/tools?project=<slug>  → 200 {nodes:[...], edges:[...], updated_at:<iso|null>} (200 with empty arrays for a never-saved project)
  - PUT  /api/v1/tools?project=<slug>  body {nodes:[...], edges:[...]} → 200 {updated_at:<iso>}; malformed → 400; oversized → 413
  - DELETE /api/v1/tools?project=<slug> → 200 {deleted:true} | 404
  - CORS: loopback-only single ACAO; OPTIONS advertises GET, POST, PUT, DELETE, OPTIONS
  - the project slug MUST match ^[a-z0-9][a-z0-9_-]{0,63}$ — project IDs from data.jsx already use this lowercase-hyphen shape (_id_for)

Fetch scaffolding to mirror (from frontend/pages/folders.jsx):
  - const API_BASE = "http://127.0.0.1:8765";                              (line 14)
  - getToken(): URLSearchParams ?token= → window.INVISIBLE_TOKEN → ""      (lines 23-28)
  - inside a useEffect keyed on the project:                               (lines 114-124)
      const token = getToken();
      const headers = token ? { Authorization: "Bearer " + token } : {};
      const qs = "?project=" + encodeURIComponent(<projId>);
      let cancelled = false;
      fetch(API_BASE + "/api/v1/tools" + qs, { headers })
        .then(r => r.json().then(body => ({ ok: r.ok, status: r.status, body })))
        .then(({ ok, status, body }) => { if (cancelled) return; /* setState */ })
        .catch(err => { if (cancelled) return; /* setError */ });
      return () => { cancelled = true; /* + clearTimeout pending autosave */ }; (lines 191-196)

Tools.jsx current shape (the code being modified):
  - React hooks aliased at line 4: useStateTL/useRefTL/useEffectTL/useMemoTL
  - ToolsCanvas({initialNodes, initialEdges, accentC}) owns nodes/edges internally (lines 33-47); re-seeds on prop change via useEffectTL([initialNodes, initialEdges])
  - canvas mutators that must trigger autosave: onDrop (84-92), drag-end via the window `up` handler (60), endConnect add-edge (75-82), removeSelected (95-100)
  - the toolbar chip "{nodes.length} nodes · {edges.length} wires" is at line 181 — the footer indicator goes near here
  - ProjectPicker reads TOOL_WORKFLOWS[p.id] at line 203 and renders counts at line 216 (MUST change — D-15)
  - Tools body reads TOOL_WORKFLOWS[projId] at line 236 and forces a fresh canvas with key={projId} at line 295 (MUST change to use the fetched workflow — D-15/D-13)

data.jsx removal targets (verified line numbers):
  - const TOOL_WORKFLOWS = { ... }; spans lines 171-252 (closing }; at 252)
  - Object.assign(window, { DATA_SETS, FOLDERS, TOOL_WORKFLOWS, TERM_CONTEXT }); is line 461 — drop the TOOL_WORKFLOWS token
  - leave line 460 Object.assign(window, { ANALYTICS }); and the fetchProjects fetcher (464-478) untouched
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load on switch + debounced autosave + status footer in tools.jsx</name>
  <files>frontend/pages/tools.jsx</files>
  <read_first>
    - frontend/pages/tools.jsx:33-47 (ToolsCanvas internal nodes/edges state + the re-seed-on-prop-change effect — the spot to add an onChange notification for autosave)
    - frontend/pages/tools.jsx:60,75-100 (the mutators that must trigger autosave: drag-end `up`, endConnect, onDrop, removeSelected)
    - frontend/pages/tools.jsx:176-185 (the toolbar + the "{nodes.length} nodes · {edges.length} wires" chip at 181 — where the footer indicator lives)
    - frontend/pages/tools.jsx:228-303 (the Tools component: projId derivation 229, the mock read at 236, the proj-tabs strip 240-264, and ToolsCanvas mount with key={projId} 294-299)
    - frontend/pages/folders.jsx:14,23-28,114-124,191-196 (API_BASE, getToken, header construction, the cancelled-flag fetch + cleanup — the exact load template AND the cleanup mechanism that prevents A-saves landing on B)
    - 01-PATTERNS.md lines 245-296 (the tools.jsx pattern assignment: fetch template, canvas-seeding, ProjectPicker/Tools mock reads to change)
    - 01-01-SUMMARY.md (confirm the exact GET/PUT response envelope + the slug shape shipped by the backend)
  </read_first>
  <action>
    Add the fetch scaffolding at the top of tools.jsx mirroring folders.jsx: `const API_BASE = "http://127.0.0.1:8765";` and a `getToken()` helper (URLSearchParams ?token= → window.INVISIBLE_TOKEN → ""). Build headers as `token ? { Authorization: "Bearer " + token } : {}` (Deferred: token handling follows folders' approach — do not invent a token UX).

    Load on switch (D-13): in the Tools component, add a useStateTL for the loaded workflow `{nodes, edges, updated_at}` (initialize to {nodes:[], edges:[], updated_at:null}) plus loading/error state, and a useEffectTL keyed on `[projId]` that, when projId is non-null, fetches `API_BASE + "/api/v1/tools?project=" + encodeURIComponent(projId)` with the headers, parses `{nodes,edges,updated_at}`, and sets the workflow state. Use a `let cancelled = false` flag and a cleanup `return () => { cancelled = true; ... }` exactly like folders so a slow fetch for project A can never paint after switching to B. Feed the fetched nodes/edges into ToolsCanvas as initialNodes/initialEdges and keep the existing `key={projId}` remount so the canvas re-seeds per project. Remove the line-236 `TOOL_WORKFLOWS[projId]` read — derive `wf.name` from the project (e.g. `${project.name} · workflow`) and nodes/edges from the fetched state.

    Lift change notification for autosave (D-14): ToolsCanvas currently owns nodes/edges internally. Add an `onChange(nodes, edges)` callback prop and call it whenever nodes or edges change (the simplest correct approach: a useEffectTL inside ToolsCanvas keyed on `[nodes, edges]` that calls `onChange?.(nodes, edges)`, guarded so the initial seed does not immediately fire a redundant save — e.g. skip the first run via a useRefTL "mounted" flag, or compare against initialNodes/initialEdges). The autosave-trigger points are the existing mutators (onDrop 84-92, drag-end via `up` 60, endConnect 75-82, removeSelected 95-100); routing through the nodes/edges effect covers all of them.

    Debounced PUT (D-14): in the Tools parent, keep a `useRefTL` timer. In the onChange handler, `clearTimeout(timer.current)` then `timer.current = setTimeout(() => { ...PUT... }, 1000)`. The PUT: `fetch(API_BASE + "/api/v1/tools?project=" + encodeURIComponent(projId), { method: "PUT", headers: { ...headers, "Content-Type": "application/json" }, body: JSON.stringify({ nodes, edges }) })`, then on success read `{updated_at}` and store it for the footer. Set a "saving" status when the timer fires the PUT and a "saved" (with the returned updated_at) on success, "error" on failure. Bind the target project at fire-time: capture projId in a ref (e.g. projIdRef.current, kept in sync via an effect) and read it inside the setTimeout callback so the PUT body and the ?project= query always name the same project even if a render is mid-flight — this closes the last cross-project-save race (cancel-on-switch below remains the primary guard).

    Flush/cancel on switch (D-14, CRITICAL): in the load effect's cleanup (the same `[projId]` effect or a dedicated one), `clearTimeout(timer.current)` so a pending save for project A is cancelled before B loads — a save for A must never land on B. (Flush-then-cancel is acceptable too, but the safe default is cancel-on-switch; do not fire a trailing PUT against the new projId with A's nodes.)

    Status footer (D-14): render a small indicator near the toolbar chip at line 181 (or in the proj-tabs right-aligned area near line 261-263) showing "saving…" while a PUT is in flight, "saved Ns ago" derived from the stored updated_at (relative-time formatting is Claude's discretion, within the page's existing design tokens — reuse `chip`/`mono` classes), and an error state ("save failed") on PUT failure. Keep copy minimal.
  </action>
  <acceptance_criteria>
    <source>load wired: grep -n 'fetch(API_BASE + "/api/v1/tools' frontend/pages/tools.jsx matches a GET in a projId-keyed effect; grep -c "API_BASE" frontend/pages/tools.jsx >= 1</source>
    <source>autosave PUT wired: grep -n 'method: "PUT"' frontend/pages/tools.jsx matches, and grep -n "setTimeout" + grep -n "clearTimeout" both match (the 1s debounce + cancel)</source>
    <source>flush/cancel on switch: the projId-keyed effect cleanup calls clearTimeout on the autosave timer ref (inspect the return () => {...} of the effect)</source>
    <source>footer present: grep -niE "saving|saved" frontend/pages/tools.jsx matches the indicator copy</source>
    <source>no mock read remains in tools.jsx: grep -c "TOOL_WORKFLOWS" frontend/pages/tools.jsx returns 0 after Task 2's ProjectPicker fix — for THIS task, confirm the line-236 Tools-body read is replaced by the fetched workflow</source>
    <behavior>real-browser behavior is verified in the dedicated checkpoint task (Task 3): GET fires on switch with no CORS error, PUT fires debounced after a node add, footer moves saving→saved, reload persists, project switch does not bleed state</behavior>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/tools-page && node --check frontend/pages/tools.jsx 2>/dev/null && echo "syntax ok" || node -e "require('@babel/core')" 2>/dev/null && echo "babel-check skipped (JSX)" ; grep -q 'method: "PUT"' frontend/pages/tools.jsx && grep -q 'fetch(API_BASE + "/api/v1/tools' frontend/pages/tools.jsx && grep -q "clearTimeout" frontend/pages/tools.jsx && echo "wiring grep ok"</automated>
    <human-check>Behavior is observed in Task 3 (real-browser drive). This task's gate is the source-assertion greps above.</human-check>
  </verify>
  <done>tools.jsx fetches the workflow on projId change with a cancellation guard (D-13), autosaves nodes/edges via a 1s-debounced PUT that is cancelled on project switch (D-14), and renders a saving/saved/error footer (D-14); the Tools-body mock read at line 236 is gone (D-15, completed alongside Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Remove TOOL_WORKFLOWS mock + fix ProjectPicker preview + .gitignore</name>
  <files>frontend/pages/tools.jsx, frontend/data.jsx, .gitignore</files>
  <read_first>
    - frontend/data.jsx:170-252 (the entire `const TOOL_WORKFLOWS = { ... };` block to delete — opens at 171, closes at 252)
    - frontend/data.jsx:460-461 (the two Object.assign(window,...) lines; drop only the TOOL_WORKFLOWS token from line 461, keep ANALYTICS/DATA_SETS/FOLDERS/TERM_CONTEXT)
    - frontend/data.jsx:464-478 (fetchProjects — leave untouched; confirms the no-bundler window.<X> + fetch convention)
    - frontend/pages/tools.jsx:202-217 (ProjectPicker reads TOOL_WORKFLOWS[p.id] at 203 and renders the counts at 216 — the call site that ReferenceErrors once the mock is gone)
    - .gitignore (tail — the Tauri/Node block; append workflows/ as per-machine state, D-16)
    - 01-PATTERNS.md lines 300-347 (the data.jsx removal pattern + the window.<GLOBAL> convention: removing a mock = delete the const AND its Object.assign token AND every reader)
  </read_first>
  <action>
    Delete the entire `const TOOL_WORKFLOWS = { ... };` block in frontend/data.jsx (lines 171-252 inclusive, through its closing `};`) and the leading comment at line 170 (D-15). Edit line 461 from `Object.assign(window, { DATA_SETS, FOLDERS, TOOL_WORKFLOWS, TERM_CONTEXT });` to `Object.assign(window, { DATA_SETS, FOLDERS, TERM_CONTEXT });` — remove only the TOOL_WORKFLOWS token. Do NOT touch line 460 (ANALYTICS) or the fetchProjects block (464-478).

    Fix the ProjectPicker reader in frontend/pages/tools.jsx (D-15): the picker reads `const wf = TOOL_WORKFLOWS[p.id]` at line 203 and renders `${wf.nodes.length} nodes · ${wf.edges.length} wires` at line 216 — both break once the mock is gone. Replace with a neutral label that needs no mock — render "open to view" (or similar neutral copy within existing design tokens) in place of the counts, and remove the `wf`/`has` references that depend on TOOL_WORKFLOWS. (Fetching per-card counts is allowed per D-15 but the neutral label is the minimal correct fix; do not add N fetches to the picker grid.) After this edit, `grep -c "TOOL_WORKFLOWS" frontend/pages/tools.jsx` MUST return 0.

    Add `workflows/` to .gitignore (D-16): append a `workflows/` entry (with a brief comment, e.g. "# per-machine tool-workflow state (daemon-created under repo root)") so the per-project JSON files the daemon writes when run with INVISIBLE_HOME=$(pwd) inside this worktree are never committed.
  </action>
  <acceptance_criteria>
    <source>mock const gone: grep -c "const TOOL_WORKFLOWS" frontend/data.jsx returns 0</source>
    <source>window export cleaned: grep -n "TOOL_WORKFLOWS" frontend/data.jsx returns nothing (zero matches in the whole file)</source>
    <source>untouched siblings intact: grep -q "Object.assign(window, { ANALYTICS });" frontend/data.jsx AND grep -q "fetchProjects" frontend/data.jsx both succeed</source>
    <source>no mock reader left in tools.jsx: grep -c "TOOL_WORKFLOWS" frontend/pages/tools.jsx returns 0</source>
    <source>gitignore updated: grep -c "^workflows/" .gitignore returns 1</source>
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/tools-page && test "$(grep -c TOOL_WORKFLOWS frontend/data.jsx)" = "0" && test "$(grep -c TOOL_WORKFLOWS frontend/pages/tools.jsx)" = "0" && grep -q "^workflows/" .gitignore && grep -q "Object.assign(window, { ANALYTICS });" frontend/data.jsx && echo "removal + gitignore ok"</automated>
  </verify>
  <done>TOOL_WORKFLOWS is fully removed from data.jsx (const + window token) with siblings intact, the ProjectPicker preview no longer reads the mock (no ReferenceError), and workflows/ is gitignored (D-15, D-16).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-browser end-to-end verification (drive with Playwright / chrome-devtools)</name>
  <what-built>
    Backend (Plan 01-01) serves real per-project workflows with a loopback-only single-ACAO CORS fix; the Tools page (Tasks 1-2 of this plan) loads on project switch, autosaves with a 1s debounce + footer, and no longer reads the deleted TOOL_WORKFLOWS mock.
  </what-built>
  <read_first>
    - 01-CONTEXT.md lines 121-122 (the MANDATORY real-browser verification protocol — observed behavior, not a manual checklist)
    - ~/.claude memory `feedback_verify_yourself` (drive the app with Playwright/chrome-devtools after UI/daemon changes; the user wants observations)
    - frontend/pages/folders.jsx:14 (API_BASE confirms frontend expects the daemon on :8765; the frontend itself is served on :8090)
  </read_first>
  <how-to-verify>
    Drive the running app with Playwright (or chrome-devtools-mcp) — do NOT hand back a manual checklist. Bring up both servers first:
    1. Start the dashboard daemon against a scratch home so it cannot touch real state:
       `INVISIBLE_HOME="$(pwd)" ./bin/invisible-dashboard --no-auth --port 8765 &`  (workflows/ will be created under the repo root — it is gitignored per D-16).
       Start the frontend on :8090 the same way the project already serves it (check the repo's existing serve command / static server for frontend/).
    2. Open the Tools page in the browser. Pick a project. ASSERT via the network panel: a GET /api/v1/tools?project=<id> fires and returns 200 with NO CORS error in the console (this proves the D-11/D-12 fix). Capture the request/response.
    3. Add or drag a node on the canvas. ASSERT: after ~1s a single PUT /api/v1/tools?project=<id> fires (debounce — not one-per-mousemove), the footer transitions "saving…" → "saved" with a timestamp. Capture the PUT.
    4. Reload the page (same project). ASSERT: the added node persists (the canvas seeds from the GET, proving the round-trip).
    5. Switch to a different project via the tab strip, then back. ASSERT: each project loads its own workflow (no state bleed — the second project does not show the first's nodes), and no stray PUT for project A lands on project B (watch the network panel during the switch — the pending debounce is cancelled).
    Record the observed results (status codes, the single PUT, the persisted node, the clean per-project load) in the SUMMARY. If any assertion fails (e.g. a CORS error, a save for A landing on B, or no persistence), capture the console/network evidence and flag it as a gap rather than approving.
  </how-to-verify>
  <resume-signal>Type "approved" once the five assertions are observed in the browser, or describe the failing assertion with the captured console/network evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tools page (browser) → dashboard API (:8765) | the page now sends authenticated GET/PUT/DELETE; it trusts the now-hardened API from Plan 01-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INV02-01 | Tampering (cross-project save) | debounced autosave timer | mitigate | D-14 cancel/flush the pending timer in the projId-effect cleanup so a save for project A cannot land on B; verified live in Task 3 (switch projects, confirm no stray PUT for A). |
| T-INV02-02 | Information Disclosure (token handling) | getToken() / Authorization header | accept | Follows folders.jsx exactly (token from ?token= or window.INVISIBLE_TOKEN); a real token UX is explicitly out of scope (CONTEXT Deferred Ideas). No new exposure beyond the existing sibling pages. |
| T-INV02-03 | Tampering (server-side traversal/validation) | the project slug + payload sent to the API | mitigate (upstream) | All server-side trust-boundary enforcement (slug regex, body cap, list-validation, atomic write) lives in Plan 01-01; this client-side plan introduces no new HIGH threat and relies on the hardened API. |
| T-INV02-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | No new packages — the frontend is Babel-standalone React with globals on window (no bundler, no install step). No package-legitimacy checkpoint required. |

No new HIGH server-side threat is introduced by this client-side plan; the one client-side tampering vector (cross-project save) is mitigated by D-14 and verified in the browser (Task 3).
</threat_model>

<verification>
Source-assertion gate (Tasks 1-2):
```bash
cd /Users/ace/.invisible-ws/tools-page
grep -q 'fetch(API_BASE + "/api/v1/tools' frontend/pages/tools.jsx        # load wired (D-13)
grep -q 'method: "PUT"' frontend/pages/tools.jsx                          # autosave PUT (D-14)
grep -q "clearTimeout" frontend/pages/tools.jsx                           # debounce + cancel-on-switch (D-14)
grep -niE "saving|saved" frontend/pages/tools.jsx                         # footer (D-14)
test "$(grep -c TOOL_WORKFLOWS frontend/data.jsx)" = "0"                  # mock removed (D-15)
test "$(grep -c TOOL_WORKFLOWS frontend/pages/tools.jsx)" = "0"           # no reader left (D-15)
grep -q "^workflows/" .gitignore                                         # gitignore (D-16)
```

Real-browser gate (Task 3): the five observed assertions (GET-on-switch no CORS error, debounced single PUT, footer saving→saved, reload persistence, clean per-project load with no A→B bleed) recorded with network/console evidence in the SUMMARY.
</verification>

<success_criteria>
- ROADMAP Success Criterion 4: the canvas loads from /api/v1/tools on project switch, saves on node add/drag/wire change debounced 1s, and renders a saving/saved footer — D-13, D-14.
- ROADMAP Success Criterion 5: switching projects loads that project's workflow without bleeding state — D-14 (cancel-on-switch) + the key={projId} remount, observed in Task 3.
- TOOL_WORKFLOWS is gone with both call sites fixed (no ReferenceError) — D-15.
- workflows/ is gitignored — D-16.
- The cross-origin load works with no CORS error (depends on 01-01's D-11/D-12), observed in a real browser.
</success_criteria>

<output>
Create `.planning/workstreams/tools-page/phases/INV-01-api-v1-tools-crud-tools-page-wired/01-02-SUMMARY.md` when done — include the recorded browser observations (status codes, the single debounced PUT, the persisted node, the clean per-project load).
</output>
