---
phase: INV-01-ssh-controlmaster-invisible-toml-host
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/api/tree_vps.py
  - tests/test_tree_vps.py
  - tests/test_tree_vps_live.py
  - tests/conftest.py
  - pytest.ini
autonomous: true
requirements:
  - REQ-VPS-01
  - REQ-04
requirements_addressed:
  - REQ-VPS-01
  - REQ-04

must_haves:
  truths:
    - "When `~/.invisible/invisible.toml` has `vps.host = \"srv982719\"`, a live `lib.api.tree_vps.walk_all(project='<configured-project>')` call returns `([{...tree...}], 200)` — NOT `([VPS_NOT_CONFIGURED], 503)`."
    - "When `~/.invisible/invisible.toml` has `vps.host = \"\"`, `walk_all()` still returns `([VPS_NOT_CONFIGURED], 503)` exactly as it does today (regression guard for the graceful-degradation contract)."
    - "When `vps.host = \"srv982719\"` but no project has a `vps_repo_path` set, `walk_all()` returns `([], 200)` (per-project opt-in semantics, NOT a 503)."
    - "When `vps.host = \"srv982719\"` and a project has `vps_repo_path = \"/srv/<dir-that-does-not-exist>\"`, the returned project node has `badge = \"unreachable\"` and the daemon does NOT crash."
    - "When called with `project='<unknown>'`, `walk_all(project='__nope__')` returns `([], 200)` regardless of `vps.host` value (cross-walker BLOCKER #2 contract preserved)."
    - "The new pytest integration test `tests/test_tree_vps_live.py` runs against the live `srv982719` when the `--integration` flag is passed AND `srv982719` is reachable (verified by a fixture that runs `ssh -o BatchMode=yes -o ConnectTimeout=4 srv982719 true` once per session); it SKIPS cleanly (not FAILS) when either condition is missing."
    - "All existing unit tests for `tree_vps` (already hermetic, monkeypatching `INVISIBLE_HOME` and stubbing `subprocess.run`) continue to pass — confirming this plan did not silently break any of the existing contracts."
    - "The total walk time for a single project with `vps_repo_path = \"/srv/<project>\"` against `srv982719` is under 3 seconds (warm — ControlMaster master already established from Plan 01-01's setup)."
    - "Any logged error from a failed remote `find` is one line, contains no remote stack trace, and does NOT echo the contents of `vps.identity` (path is fine; file bytes are not)."
  artifacts:
    - path: "lib/api/tree_vps.py"
      provides: "Existing sophisticated SSH walker, with surgical hardening if integration tests surface gaps. Not a rewrite."
      contains: "VPS_NOT_CONFIGURED"
    - path: "tests/test_tree_vps.py"
      provides: "Hermetic unit tests that stub subprocess.run and assert each branch of walk_all + _walk_remote returns the documented shape. NO live SSH."
      contains: "def test_walk_all_empty_host_returns_503"
    - path: "tests/test_tree_vps_live.py"
      provides: "Integration tests gated behind the --integration pytest flag AND a session-scoped reachability fixture. Hits the live srv982719."
      contains: "def test_walk_all_live_returns_tree"
    - path: "tests/conftest.py"
      provides: "Session-scoped fixture `vps_reachable` that runs the BatchMode probe once and SKIPs all live tests when False. Also registers the `vps_integration` marker."
      contains: "vps_reachable"
    - path: "pytest.ini"
      provides: "Marker registration so unknown-marker warnings don't fire; `--integration` CLI flag definition."
      contains: "vps_integration"
  key_links:
    - from: "tests/test_tree_vps_live.py"
      to: "lib/api/tree_vps.py walk_all"
      via: "direct import"
      pattern: "from api.tree_vps import"
    - from: "tests/conftest.py vps_reachable"
      to: "real OpenSSH binary on PATH"
      via: "subprocess.run with srv982719 alias from ~/.ssh/config"
      pattern: "subprocess.run.*ssh.*srv982719"
    - from: "lib/api/tree_vps.py _walk_remote"
      to: "remote /srv/<project> via SSH"
      via: "subprocess.run argv-form (no shell)"
      pattern: "subprocess.run.*_ssh_argv"
---

<objective>
Verify the EXISTING `lib/api/tree_vps.py` SSH walker against the live `srv982719` VPS after Plan 01-01 wired the SSH alias. Add hermetic unit tests for every documented branch in `walk_all` + `_walk_remote` (these don't exist yet for this module — they were planned but not committed during M1 per `.planning/CONTEXT.md`), and a guard-railed integration test that hits the real VPS only when reachable.

This plan ACKNOWLEDGES the launch-prompt framing ("you rewrite that path") is outdated. The orchestrator and this planner both READ `lib/api/tree_vps.py` line-by-line (411 lines, fully documented docstrings). The file is:
- Already ControlMaster-aware at the subprocess argv layer (`_ssh_argv()` lines 170-190).
- Already validating hosts and paths defensively (`_validate_host()` lines 122-126, `_validate_remote_path()` lines 129-142, with T-INV01-07 / T-INV01-09 callouts in the docstring).
- Already implementing the graceful-degradation contract: `host == ""` returns `([VPS_NOT_CONFIGURED], 503)` (lines 363-364).
- Already preserving the cross-walker BLOCKER #2 contract: `walk_all(project='__nope__')` returns `([], 200)` regardless of host (lines 351-354).
- Already returning `badge = "unreachable"` on SSH failures (lines 263-282) — the dashboard never crashes.
- Already implementing depth-bounded `find` walks with ignore-list pruning (lines 246-254).

What this plan WILL do:
1. Run the existing module against the live `srv982719` from a unit-test entry point (Task 1: integration test scaffolding).
2. If the integration test surfaces a CONCRETE failure path (a specific `_walk_remote()` branch that breaks on the real VPS), add a minimal targeted fix to `lib/api/tree_vps.py` AND a regression unit test (Task 2).
3. Ship hermetic unit tests for every documented branch of `walk_all` so future maintainers can refactor with confidence (Task 3).
4. NOT rewrite the file. Surgical edits only.

What this plan WILL NOT do:
- Replace `_ssh_argv()` (the existing ControlMaster setup is correct).
- Move the per-walker ControlPath from `$INVISIBLE_HOME/run/ssh-cm-*` to `~/.ssh/cm-*` (Decision A in Plan 01-01 documents why they coexist).
- Touch `lib/pty_server.py` (Phase 3 territory).
- Touch any frontend file.

Purpose: Phase 1 acceptance pivot from "code change needed" to "config + verify" — the headline shift since the launch prompt was written. After this plan, the next phase can deploy `invisible-server` knowing the SSH plumbing is solid end-to-end.

Output:
- New file `tests/test_tree_vps.py` (hermetic unit tests, no live SSH).
- New file `tests/test_tree_vps_live.py` (integration tests, gated).
- New / extended `tests/conftest.py` (registering the `vps_integration` marker and the `vps_reachable` session fixture).
- New / extended `pytest.ini` (marker registration + `--integration` flag).
- Surgical edits to `lib/api/tree_vps.py` IF AND ONLY IF the integration tests surface a concrete failure — accompanied by a regression unit test that fails before the fix and passes after.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/CONTEXT.md
@.planning/workstreams/vps-connection/ROADMAP.md
@.planning/workstreams/vps-connection/STATE.md
@.planning/workstreams/vps-connection/phases/INV-01-ssh-controlmaster-invisible-toml-host/01-01-PLAN.md
@START_HERE.md

@lib/api/tree_vps.py
@lib/config.py
@invisible.toml.example
@tests/test_api_projects.py

<interfaces>
Extracted from lib/api/tree_vps.py — the complete public + private surface. The executor MUST treat this as the source of truth and NOT re-derive it from the file. Reading the file once to confirm is fine; reading repeatedly to "double-check" is wasted context budget.

```
# CONSTANTS (module-level)
VPS_NOT_CONFIGURED: dict[str, str] = {"error": "vps.host not configured"}
MAX_DEPTH = 6
SSH_TIMEOUT_S = 15
_IGNORE_DIRS: tuple[str, ...] = (".git", "node_modules", ".venv", "venv", "__pycache__")
_HOST_RE = re.compile(r"^(?:[A-Za-z0-9_.-]+@)?[A-Za-z0-9._-]+$")
_PATH_RE = re.compile(r"^/[A-Za-z0-9_./~+-]+$")

# VALIDATION (defense-in-depth — argv-form exec already blocks injection)
def _validate_host(host: str) -> bool
def _validate_remote_path(p: str) -> bool

# CONTROLMASTER SOCKET (creates $INVISIBLE_HOME/run/ if missing)
def _cm_path() -> str

# SSH ARGV BUILDER (lines 170-190 — NEVER invokes a shell)
def _ssh_argv(host: str, identity: str, *remote_cmd: str) -> list[str]:
    # Returns:
    # ["ssh", "-o", "BatchMode=yes",
    #  "-o", "ConnectTimeout=15",
    #  "-o", "ControlMaster=auto",
    #  "-o", f"ControlPath={$INVISIBLE_HOME/run/ssh-cm-%r@%h:%p}",
    #  "-o", "ControlPersist=60s",
    #  "-i", os.path.expanduser(identity),
    #  host,
    #  "--",
    #  *remote_cmd]

# TREE ASSEMBLY (twin of tree_repo._insert)
def _insert(node_list: list[dict[str, Any]], parts: list[str], kind: str) -> None
    # kind is "f" (file) or "d" (directory) — from `find -printf "%y %p\n"`

# REMOTE WALKER
def _walk_remote(host: str, identity: str, remote_root: str) -> dict[str, Any]
    # Returns one tree node.
    # On any SSH or find failure, returns a placeholder with badge="unreachable".
    # Filters lines: only "f" and "d" kinds; skips the root itself; skips lines
    # whose path doesn't start with remote_root (defense-in-depth).

# PUBLIC API
def walk_all(project: str | None = None) -> tuple[list[dict[str, Any]], int]
    # Return-shape contract (evaluated in this order):
    # 1. ([], 200)               — project given but doesn't match any configured project
    # 2. ([VPS_NOT_CONFIGURED], 503) — vps.host empty
    # 3. ([{"error": ...}], 503) — vps.host fails _validate_host regex
    # 4. ([...], 200)            — happy path
```

The on-VPS layout per Mac memory `vps_infra.md`:

```
HostName:     31.97.222.218
User:         avi
Per-app code: /srv/<app>/  (owned by avi:avi)
```

So a `vps_repo_path` that exists today on the real VPS would be (for example) `/srv/bg-remover` or `/srv/invisible`. The user's `~/.invisible/invisible.toml` will need at least one `[[projects]]` entry with `vps_repo_path = "/srv/<something-that-actually-exists>"` for the integration test to assert a non-empty tree. The integration test must NOT hard-code a specific `/srv/...` path; it should DISCOVER one from the user's `invisible.toml` and SKIP if none is configured.

The existing hermetic unit-test pattern (lifted from tests/test_api_projects.py):

```
# tests/test_api_projects.py establishes the precedent:
# - sys.path.insert(0, str(LIB)) where LIB = HERE.parent / "lib"
# - tmp_path fixture for $INVISIBLE_HOME isolation
# - monkeypatch.setenv("INVISIBLE_HOME", str(tmp_path))
# - synthetic invisible.toml writer (_write_toml helper)
# - stdlib-only imports except pytest itself
```

This plan's new `tests/test_tree_vps.py` reuses the SAME pattern: tmp_path + monkeypatch + synthetic invisible.toml + stub the `subprocess.run` calls inside `_walk_remote`. NO live SSH in the hermetic file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hermetic unit tests for tree_vps.walk_all (no live SSH)</name>
  <files>tests/test_tree_vps.py</files>
  <read_first>
    - lib/api/tree_vps.py (CURRENT STATE — sophisticated SSH walker; you are NOT modifying it in this task, you are ASSERTING its existing contracts)
    - tests/test_api_projects.py (sibling test file — same fixture pattern: sys.path insertion, tmp_path, monkeypatch INVISIBLE_HOME, synthetic invisible.toml writer)
    - lib/config.py (home(), load_toml() — the readers tree_vps depends on)
  </read_first>
  <action>
    Create `tests/test_tree_vps.py` mirroring the structure of `tests/test_api_projects.py`. Every test name describes the exact contract from the `lib/api/tree_vps.py` docstring lines 326-340 that it asserts.

    Test module preamble (verbatim from test_api_projects.py):
    - `from __future__ import annotations`
    - `import json`, `import subprocess`, `import sys`, `from pathlib import Path`, `import pytest`
    - `HERE = Path(__file__).resolve().parent`; `LIB = HERE.parent / "lib"`; insert LIB on sys.path if not present.

    Helper functions (lift the patterns from test_api_projects.py):
    - `_write_toml(home: Path, vps: dict | None, projects: list[dict]) -> None` — write a `[vps]` block (if vps is not None) plus `[[projects]]` blocks into `home / "invisible.toml"`.
    - `_make_walker(monkeypatch, tmp_path, *, vps: dict | None, projects: list[dict]) -> module`:
        1. monkeypatch.setenv("INVISIBLE_HOME", str(tmp_path))
        2. _write_toml(tmp_path, vps, projects)
        3. import `api.tree_vps` AFTER the env override — use `importlib.reload(api.tree_vps)` to dodge module-caching across tests.
        4. Return the imported module.

    Stub strategy for subprocess.run inside `_walk_remote`:
    - Pattern: `monkeypatch.setattr("api.tree_vps.subprocess.run", fake_run)` where `fake_run(argv, **kw)` returns a `subprocess.CompletedProcess` with stdout matching the expected `find -printf "%y %p\n"` format. Each test defines its own `fake_run` closure so test_3 can simulate timeout, test_4 can simulate non-zero rc, etc.

    Tests to implement (one function each, named for the contract):

    ### Branch coverage of `walk_all`:

    1. `test_walk_all_empty_host_returns_503` — vps={"host": ""}; one project with vps_repo_path; assert returns `(VPS_NOT_CONFIGURED_list, 503)`. Stub fake_run so we ASSERT it was NEVER called (the short-circuit must happen before SSH).

    2. `test_walk_all_missing_vps_section_returns_503` — vps=None (no [vps] block at all); same assertion as test 1.

    3. `test_walk_all_invalid_host_returns_503_with_error_body` — vps={"host": "bad;rm -rf /"}; assert returns `([{"error": "invalid vps.host: 'bad;rm -rf /'"}], 503)`. (Note: the bad host content IS echoed in the error body — this is acceptable here because the error body never reaches the user's browser; the dashboard handler will strip it.)

    4. `test_walk_all_unknown_project_returns_empty_200_regardless_of_host` — vps={"host": "srv982719"}; project filter "__nope__"; assert returns `([], 200)`.

    5. `test_walk_all_unknown_project_returns_empty_200_when_host_empty` — vps={"host": ""}; project filter "__nope__"; assert returns `([], 200)` — the BLOCKER #2 contract runs BEFORE the host check.

    6. `test_walk_all_project_without_vps_repo_path_silently_skipped` — vps={"host": "srv982719"}; one project with NO `vps_repo_path` key; assert returns `([], 200)` and fake_run was never called.

    7. `test_walk_all_project_with_invalid_vps_repo_path_skipped_with_stderr` — vps={"host": "srv982719"}; one project with `vps_repo_path = "../../etc"`; capture stderr (capsys); assert that the project was skipped (returns `([], 200)`) and stderr contains "invalid vps_repo_path".

    ### Branch coverage of `_walk_remote`:

    8. `test_walk_remote_happy_path_builds_nested_tree` — fake_run returns stdout simulating:

       ```
       d /srv/proj
       d /srv/proj/src
       f /srv/proj/src/main.py
       d /srv/proj/src/utils
       f /srv/proj/src/utils/io.py
       f /srv/proj/README.md
       ```

       Assert the returned node has `name="/srv/proj"`, `type="folder"`, `open=True`, and `children` contains a folder `src` with two children (`main.py` file, `utils` folder) plus a file `README.md`. Assert there is NO "badge" key (only present on failure).

    9. `test_walk_remote_ssh_failure_returns_unreachable_badge` — fake_run returns CompletedProcess with returncode=255 (the standard SSH connection-failure rc); assert returned node has `badge = "unreachable"`, `children = []`, and `type = "folder"`. Capture stderr; assert the error was logged.

    10. `test_walk_remote_timeout_returns_unreachable_badge` — fake_run raises `subprocess.TimeoutExpired`; same assertion as test 9.

    11. `test_walk_remote_oserror_returns_unreachable_badge` — fake_run raises `OSError("ENOENT")`; same assertion as test 9.

    12. `test_walk_remote_skips_symlinks_and_other_kinds` — fake_run stdout includes `"l /srv/proj/link"` and `"s /srv/proj/sock"` lines; assert the resulting tree contains NEITHER (only `f` and `d` lines are honored, per lines 296-298 of the module).

    13. `test_walk_remote_skips_root_itself_in_children` — fake_run stdout includes `"d /srv/proj"` as the first line; assert the returned node's `children` does NOT contain a child literally named `/srv/proj` or empty-string (the root itself is consumed as the wrapping node, lines 300-303).

    14. `test_walk_remote_skips_paths_outside_root_prefix` — fake_run stdout includes a line `"f /etc/passwd"` (a paranoid attacker scenario where the remote find somehow emits an out-of-tree path); assert that line is silently dropped (lines 303-307).

    ### Top-level shape contract:

    15. `test_walk_all_happy_path_wraps_in_project_node` — vps={"host":"srv982719"}; one project with `vps_repo_path = "/srv/proj"`; fake_run returns one file under /srv/proj; assert returns `([{...}], 200)` where the top-level node has `name=<project-name>`, `type="folder"`, `open=True`, and `children = [<walked-vps-root-node>]` (matches docstring lines 27-30).

    16. `test_walk_all_argv_passes_through_ssh_options_and_identity` — vps={"host":"srv982719", "identity": "~/.ssh/custom_key"}; capture the argv passed to fake_run; assert argv[0] == "ssh", `-o BatchMode=yes`, `-o ControlMaster=auto`, `-o ControlPersist=60s`, and `-i <expanded-path-to-~/.ssh/custom_key>` all appear; assert "srv982719" appears as a positional argv element; assert "--" appears between the host and the remote command (the option-terminator from line 188).

    No test should hit the real network. The session-scoped `vps_reachable` fixture (added in Task 3) does NOT gate this file — these tests are pure stubs and MUST run on CI without an SSH path.

    Mark each test name to follow the same convention as `tests/test_api_projects.py`: lowercase snake_case starting with `test_`, describing the contract.
  </action>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest tests/test_tree_vps.py -x -q 2>&1 | tail -20</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest tests/test_tree_vps.py --collect-only -q 2>&1 | grep "test_" | wc -l</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest tests/test_tree_vps.py -k "test_walk_all_empty_host_returns_503 or test_walk_all_unknown_project_returns_empty_200_regardless_of_host or test_walk_remote_ssh_failure_returns_unreachable_badge" -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `tests/test_tree_vps.py` exists with at least 16 test functions.
    - `pytest tests/test_tree_vps.py -x -q` exits 0.
    - All 16 tests above pass — confirming the existing `lib/api/tree_vps.py` honors every documented contract.
    - The test file is hermetic: no network access, no real ssh, no `~/.ssh/config` read, no real `$INVISIBLE_HOME` mutation (all monkeypatched to tmp_path).
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test scaffolding (conftest + pytest.ini + live test file)</name>
  <files>tests/conftest.py, tests/test_tree_vps_live.py, pytest.ini</files>
  <read_first>
    - lib/api/tree_vps.py (CURRENT STATE — confirm the public API names you import; do NOT modify it in this task)
    - .planning/workstreams/vps-connection/phases/INV-01-ssh-controlmaster-invisible-toml-host/01-01-PLAN.md (the Plan 01-01 checkpoint Task 4 setup is the prerequisite — the integration test ASSUMES Plan 01-01 was completed)
    - tests/test_api_projects.py (sibling test pattern — for sys.path + tmp_path + monkeypatch conventions)
  </read_first>
  <action>
    Create three files in parallel — they form one logical unit.

    ### `pytest.ini` (create new; root of the repo, NOT inside tests/):

    Add:
    - `[pytest]` section
    - `markers` registering `vps_integration: marks tests that require a live srv982719 SSH connection (skipped without --integration)`.
    - An `addopts` line: `--strict-markers` (so unknown markers become test errors rather than warnings).

    Add the `--integration` CLI flag — register via `tests/conftest.py` (not pytest.ini, since custom CLI options live in conftest.py).

    ### `tests/conftest.py` (create new):

    1. `pytest_addoption(parser)` hook that adds `--integration` (default False, action store_true, help "Run live VPS integration tests against srv982719").

    2. `pytest_collection_modifyitems(config, items)` hook that:
       - If `--integration` was NOT passed, mark every item with the `vps_integration` marker as `pytest.mark.skip(reason="--integration not given")`.

    3. Session-scoped fixture `vps_reachable() -> bool`:
       - Run once per pytest session.
       - Execute `subprocess.run(["ssh", "-o", "BatchMode=yes", "-o", "ConnectTimeout=4", "srv982719", "true"], capture_output=True, timeout=8, check=False)`.
       - Return True iff `returncode == 0`.
       - On any exception (`TimeoutExpired`, `FileNotFoundError`, `OSError`), return False — never raise.
       - Cache the result with `@pytest.fixture(scope="session")`.

    4. Session-scoped fixture `vps_configured_project() -> tuple[str, str] | None`:
       - Read the user's REAL `~/.invisible/invisible.toml` (NOT a tmp_path one — this is the integration suite, not unit tests).
       - Walk `cfg.get("projects", [])` looking for the first entry that has BOTH a `name` AND a `vps_repo_path`.
       - Return `(name, vps_repo_path)` or `None` if no such project is configured.
       - Used by every live test to discover which project to walk.

    5. Helper: `pytest.fixture` named `live_vps_or_skip` (depends on `vps_reachable` and `vps_configured_project`) that:
       - If `vps_reachable` is False, calls `pytest.skip("srv982719 not reachable (BatchMode ssh failed) — run /gsd:plan-phase 1 Plan 01-01 first")`.
       - If `vps_configured_project` is None, calls `pytest.skip("no [[projects]] in ~/.invisible/invisible.toml has vps_repo_path set — add one and re-run")`.
       - Returns the `(name, vps_repo_path)` tuple.

    ### `tests/test_tree_vps_live.py` (create new):

    Module preamble: sys.path insertion identical to other test files.

    Mark the WHOLE module: `pytestmark = pytest.mark.vps_integration` so every test in the file is gated by the marker.

    Live tests to implement:

    1. `test_walk_all_live_returns_tree(live_vps_or_skip)`:
       - Unpack `name, vps_repo_path = live_vps_or_skip`.
       - `from api.tree_vps import walk_all`.
       - `rows, status = walk_all(project=name)`.
       - Assert `status == 200`.
       - Assert `len(rows) == 1`.
       - Assert `rows[0]["name"] == name` and `rows[0]["type"] == "folder"`.
       - Assert `rows[0]["children"][0]["name"] == vps_repo_path` (the walked-vps-root-node).
       - Assert `rows[0]["children"][0]["type"] == "folder"`.
       - Assert no `"badge"` key on the walked-root-node (badge only appears on failure).
       - Assert `rows[0]["children"][0]["children"]` is a non-empty list (there's SOME content under `/srv/<project>`).

    2. `test_walk_all_live_warm_call_completes_under_3s(live_vps_or_skip)`:
       - Time the SAME `walk_all(project=name)` call with `time.perf_counter()`.
       - Prime the master once with `subprocess.run(["ssh", "srv982719", "true"], check=True)` before the timer starts.
       - Assert duration < 3.0 seconds (warm-master target from the must_haves).

    3. `test_walk_all_live_empty_host_still_503(monkeypatch, tmp_path)`:
       - Even on the live system, regression-guard the graceful-degradation path. Monkeypatch INVISIBLE_HOME to tmp_path and write an invisible.toml with `host = ""`.
       - Reload `api.tree_vps`.
       - Assert `walk_all() == ([VPS_NOT_CONFIGURED], 503)`.
       - This test is HERMETIC but lives in the live file because it tests the same module's other contract — and we want every contract verified together when `--integration` is on.

    4. `test_walk_all_live_unknown_path_yields_unreachable_badge(live_vps_or_skip, monkeypatch, tmp_path)`:
       - Unpack `name, _ = live_vps_or_skip`.
       - Build a synthetic invisible.toml in tmp_path with `vps.host = "srv982719"` and one project whose `vps_repo_path = "/srv/__definitely_does_not_exist_invisible_test__"`.
       - Monkeypatch INVISIBLE_HOME, reload, call walk_all.
       - Assert `status == 200` (NOT 503 — bad remote path is a per-walk failure, not a config failure).
       - Assert the wrapped project node's `children[0]` has `badge == "unreachable"`.

    5. `test_walk_all_live_argv_actually_uses_controlmaster(live_vps_or_skip)`:
       - Spy on `subprocess.run` (monkeypatch with a wrapping function that captures argv into a list before delegating to the real subprocess.run).
       - Call `walk_all(project=name)`.
       - Assert the captured argv includes `-o ControlMaster=auto`, `-o ControlPersist=60s`, and `-o ControlPath=` (followed by a path inside `$INVISIBLE_HOME/run/`).
       - Assert the captured argv includes the `--` option-terminator before the remote `find` command.
       - This test is REDUNDANT with `test_walk_all_argv_passes_through_ssh_options_and_identity` from Task 1, but the hermetic version stubs the call entirely — this one CONFIRMS the real run uses the same argv when actually invoking ssh against the live host.

    Acceptance: when `--integration` is NOT passed, `pytest -q` collects and SKIPS these 5 tests; when it IS passed AND srv982719 is reachable, all 5 PASS; when `--integration` is passed but srv982719 is unreachable, they SKIP with the "not reachable" reason (not FAIL).
  </action>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -c "import configparser; cp = configparser.ConfigParser(); cp.read('pytest.ini'); assert 'pytest' in cp.sections() or 'pytest' in cp, 'pytest.ini missing [pytest] section'; print('pytest.ini OK')"</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest --collect-only tests/test_tree_vps_live.py -q 2>&1 | tail -15</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest tests/test_tree_vps_live.py -q 2>&1 | tail -10</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest tests/ -q 2>&1 | tail -15</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && grep -E "vps_integration|--integration|vps_reachable" tests/conftest.py pytest.ini tests/test_tree_vps_live.py | wc -l</automated>
  </verify>
  <done>
    - `pytest.ini` exists with `[pytest]` section and `vps_integration` marker registered.
    - `tests/conftest.py` exists with `pytest_addoption`, `pytest_collection_modifyitems`, `vps_reachable`, `vps_configured_project`, and `live_vps_or_skip` fixtures/hooks.
    - `tests/test_tree_vps_live.py` exists with the module-level `pytestmark = pytest.mark.vps_integration` and at least 5 test functions.
    - Without `--integration`: `pytest -q` collects the live tests and reports them as SKIPPED.
    - All tests from Task 1 STILL pass (Task 2 didn't break them).
    - `pytest --strict-markers` does not emit "unknown marker" warnings — meaning the marker registration is correct.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the live integration suite and patch any regression surfaced (conditional)</name>
  <files>lib/api/tree_vps.py, tests/test_tree_vps.py</files>
  <read_first>
    - lib/api/tree_vps.py (CURRENT STATE — the file you might surgically edit; do NOT rewrite, only patch if a specific test fails)
    - tests/test_tree_vps_live.py (the live tests from Task 2; you will RUN them, not modify them)
    - tests/test_tree_vps.py (the hermetic tests from Task 1; if you patch tree_vps.py you must add a regression test here)
  </read_first>
  <action>
    Run the integration suite. This task has THREE possible outcomes; the executor chooses ONE based on actual test results, NOT speculation.

    Step 1: Run live tests.

    Execute: `python3 -m pytest tests/test_tree_vps_live.py --integration -v`

    Capture the full output. Report it in the summary regardless of outcome.

    Step 2A: If ALL 5 live tests PASS, then `lib/api/tree_vps.py` is genuinely correct against the live VPS. NO code changes are needed.
    - Do NOT touch `lib/api/tree_vps.py`.
    - Add a docstring note at the TOP of the module's docstring (insert after the existing line "ControlMaster:" paragraph at line 34) recording the verification date and the fact that the integration suite passed end-to-end on `srv982719`. Format: `"Verified end-to-end against srv982719 on YYYY-MM-DD via tests/test_tree_vps_live.py (see Plan INV-01-02)."` — one line, no PII, no commit SHA (the git log has that).
    - This is the most likely outcome given the file's existing maturity. Treat it as the SUCCESS path.

    Step 2B: If any live test FAILS with a CONCRETE diagnosable failure (not a flaky network blip — re-run once to confirm), then:
    - Diagnose the exact line(s) in `lib/api/tree_vps.py` that fail. Cite the line numbers.
    - Write the MINIMAL surgical edit to fix that specific failure. NO refactoring. NO renaming. NO file-wide changes.
    - Add a regression test to `tests/test_tree_vps.py` (the hermetic file) that fails BEFORE the fix and passes AFTER. The hermetic test must reproduce the failure mode using stubs (e.g., specific `find` stdout that triggers the bug) — NOT by hitting the live VPS.
    - Re-run BOTH `pytest tests/test_tree_vps.py` AND `pytest tests/test_tree_vps_live.py --integration` and confirm both pass.
    - In the summary, cite (a) the exact failing test name, (b) the failing assertion, (c) the line(s) of `lib/api/tree_vps.py` you patched, (d) a one-line description of the fix.

    Step 2C: If any live test FAILS with NO diagnosable cause (e.g., the SSH master died mid-test, a network blip — not a tree_vps.py bug), then:
    - Re-run the failing tests once.
    - If they pass on re-run, document the flake in the summary and move on (do NOT patch tree_vps.py).
    - If they keep failing AND the cause is environmental (not code), document the environmental cause and STOP. Do NOT patch tree_vps.py to "fix" environment issues.

    AUTHORITY LIMITS for this task:
    - You may NOT rewrite `lib/api/tree_vps.py`.
    - You may NOT change the function signatures (`walk_all`, `_walk_remote`, `_ssh_argv`, `_validate_host`, `_validate_remote_path`).
    - You may NOT remove or rename `VPS_NOT_CONFIGURED`, `MAX_DEPTH`, `SSH_TIMEOUT_S`, or `_IGNORE_DIRS`.
    - You may NOT change the return-shape contract documented in lines 326-340 of the module.
    - You MAY add a single-line docstring update (Step 2A) or apply a minimal in-function fix (Step 2B).

    The Phase 1 success criteria are SHELL-side (config + key bootstrap + reachability). The code does not need to change for the criteria to be met; this task only patches the walker if the live tests prove an actual code bug.
  </action>
  <verify>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest tests/test_tree_vps.py -x -q 2>&1 | tail -5</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -m pytest tests/test_tree_vps_live.py --integration -q 2>&1 | tail -10</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -c "import ast; ast.parse(open('lib/api/tree_vps.py').read()); print('syntax OK')"</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && python3 -c "
import sys; sys.path.insert(0, 'lib')
from api import tree_vps
# Public API surface preservation guard
assert callable(tree_vps.walk_all)
assert callable(tree_vps._walk_remote)
assert callable(tree_vps._ssh_argv)
assert callable(tree_vps._validate_host)
assert callable(tree_vps._validate_remote_path)
assert isinstance(tree_vps.VPS_NOT_CONFIGURED, dict)
assert tree_vps.VPS_NOT_CONFIGURED == {'error': 'vps.host not configured'}
assert tree_vps.MAX_DEPTH == 6
assert tree_vps.SSH_TIMEOUT_S == 15
print('public API preserved')
"</automated>
    <automated>cd /Users/ace/.invisible-ws/vps-connection && git diff --stat lib/api/tree_vps.py 2>&1 | tail -3</automated>
  </verify>
  <done>
    - All hermetic tests in `tests/test_tree_vps.py` pass.
    - All live tests in `tests/test_tree_vps_live.py` pass with `--integration` (or are SKIPPED with a clear environmental reason — never FAIL silently).
    - `lib/api/tree_vps.py` public API is unchanged: same function names, same constants, same return-shape contract.
    - If `lib/api/tree_vps.py` was edited at all, the diff is small (target: under 20 lines net change including comments) AND a hermetic regression test reproducing the original failure was added to `tests/test_tree_vps.py`.
    - If `lib/api/tree_vps.py` was NOT edited, the module's docstring carries a single-line verification note dated today.
    - The SUMMARY explicitly states which of Step 2A, 2B, 2C applied, with citations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest process → user's real `~/.invisible/invisible.toml` (in live tests) | Read-only; the live tests need to discover which project to walk |
| pytest process → live VPS srv982719 over SSH | Outbound; argv-form exec (no shell); uses Plan 01-01's ControlMaster from `~/.ssh/config` |
| pytest process → tmp_path (in hermetic tests) | Isolated; no real-state mutation |
| `lib/api/tree_vps.py` _walk_remote subprocess | Argv-form exec (`shell=False`); validated host + path; timeout-bounded |
| `lib/api/tree_vps.py` _ssh_argv → `-i <identity>` | identity value from invisible.toml is `os.path.expanduser`-ed and passed via argv — never interpolated into a shell string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INV01L-01 | Information Disclosure | Integration test logs leak the user's REAL `~/.invisible/invisible.toml` contents (project paths, etc.) into pytest stdout | mitigate | `vps_configured_project` fixture returns only `(name, vps_repo_path)`; it does NOT print the full toml. Live tests assert against the returned tuple, never `print()` it. CI logs may still show pytest's failure assertion message — accept this; the project name and `/srv/<dir>` path are not secrets. |
| T-INV01L-02 | Tampering | `_walk_remote` shell-injection via `vps_repo_path` | mitigate | Already mitigated by `_validate_remote_path()` (regex `^/[A-Za-z0-9_./~+-]+$` and explicit `..` rejection) + argv-form `subprocess.run`. The hermetic test 7 (`test_walk_all_project_with_invalid_vps_repo_path_skipped_with_stderr`) explicitly asserts that a `../../etc` path is rejected. The live test 4 (`test_walk_all_live_unknown_path_yields_unreachable_badge`) confirms that a non-existent absolute path within the allowed character class yields `badge="unreachable"` rather than a crash. |
| T-INV01L-03 | Tampering | The live integration test reads `~/.invisible/invisible.toml` directly (not via tmp_path) and could be tricked by a malicious project config to walk an unintended path | accept | The test runs ONLY when the developer explicitly passes `--integration` AND the developer wrote the invisible.toml themselves. There is no untrusted input source for the live tests. |
| T-INV01L-04 | Spoofing | A different host (not the real srv982719) responds when ssh resolves `srv982719` via the user's `~/.ssh/config` | mitigate | OpenSSH host-key verification (`StrictHostKeyChecking=ask` default) mitigates first-time MITM. Subsequent connections compare against `~/.ssh/known_hosts`. Plan 01-01 step 4 explicitly tells the user to accept the host key once. T-INV01-12 in the existing `lib/api/tree_vps.py` docstring covers this and dispositions as "accept" — we inherit that disposition. |
| T-INV01L-05 | Information Disclosure | `_walk_remote` stderr printing logged paths to `sys.stderr` may leak the user's `/srv/<project>` layout into CI logs | accept | `/srv/<project>` paths are not secrets per Mac memory `vps_infra.md` (the VPS layout is documented). The error log line is `f"[tree_vps] ssh/find failed for {host}:{remote_root}: {exc}"` — one line, no stack trace. Accept. |
| T-INV01L-06 | Denial of Service | Live integration tests hang the CI / developer terminal when srv982719 is reachable-but-slow (e.g., 10s ssh latency × N projects) | mitigate | `SSH_TIMEOUT_S = 15` caps the subprocess; the live test 2 asserts `< 3.0s` warm — failing fast surfaces the problem rather than hiding it. CI scripts using `--integration` should also pass `--timeout=30` to pytest as a per-test escape hatch (documented in the SUMMARY). |
| T-INV01L-07 | Tampering | The integration test could overwrite `~/.invisible/invisible.toml` if a future maintainer adds a `_write_toml(home, ...)` call to a LIVE test by mistake | mitigate | The hermetic tests use `tmp_path` exclusively. The live tests are forbidden from calling `_write_toml(home())` — only `_write_toml(tmp_path)` is allowed (and even then, only via `monkeypatch.setenv("INVISIBLE_HOME", str(tmp_path))` so the file lands in tmp). Code review check: any commit that introduces `_write_toml` writing to a non-tmp directory must be blocked. |
| T-INV01L-08 | Repudiation | No audit trail of which test run successfully walked the live VPS | accept | Pytest log is the audit trail; the SUMMARY records the date and result. Single-user, single-machine constraint per PROJECT.md. |
| T-INV01L-SC | Tampering | npm/pip/cargo install during this plan | accept | No new package installs. pytest is already a dev-dependency. Plan adds only stdlib (`subprocess`, `time`, `pathlib`, `importlib`) and pytest fixtures. No supply-chain surface added. |
</threat_model>

<verification>
End-to-end Plan 01-02 acceptance run:

```
cd /Users/ace/.invisible-ws/vps-connection

# 1. Hermetic suite stays green (regression guard for the existing walker)
python3 -m pytest tests/test_tree_vps.py -x -q && echo "OK · hermetic suite passes"

# 2. Full test suite without --integration: live tests SKIPPED, all else passes
python3 -m pytest tests/ -q 2>&1 | grep -E "(passed|skipped|failed|error)" | tail -3
# Expected: "X passed, Y skipped" where Y >= 5 (the live tests)

# 3. Full test suite WITH --integration: live tests run and pass (if srv982719 reachable)
python3 -m pytest tests/test_tree_vps_live.py --integration -v 2>&1 | tail -15
# Expected: all 5 live tests pass, OR SKIPPED with clear "srv982719 not reachable" or "no vps_repo_path configured" reason

# 4. Public API surface guard (must run regardless of integration mode)
python3 -c "import sys; sys.path.insert(0, 'lib'); from api import tree_vps; assert tree_vps.VPS_NOT_CONFIGURED == {'error': 'vps.host not configured'}; assert tree_vps.MAX_DEPTH == 6; assert tree_vps.SSH_TIMEOUT_S == 15; print('OK · public API preserved')"

# 5. tree_vps.py diff size guard (surgical edits only)
git diff --shortstat lib/api/tree_vps.py 2>&1
# Expected: either "0 files changed" (Step 2A — no code changes) or a small diff (Step 2B — under 20 lines net)

# 6. Live VPS reachability still works after the test run (ControlMaster wasn't broken)
ssh -o BatchMode=yes -o ConnectTimeout=4 srv982719 echo ok | grep -q "^ok$" && echo "OK · live SSH still works"
```

Lines 1, 4, 5, 6 must always print `OK · ...`. Lines 2 and 3 are informational — the SUMMARY records the actual numbers.
</verification>

<success_criteria>
- All 16 hermetic tests in `tests/test_tree_vps.py` pass.
- All 5 live tests in `tests/test_tree_vps_live.py` either pass (when `--integration` + reachable srv982719) or SKIP cleanly (otherwise) — never FAIL silently.
- `lib/api/tree_vps.py` public API surface (function names, constants, return-shape contract) is unchanged.
- If any surgical edit to `lib/api/tree_vps.py` was needed, it is accompanied by a hermetic regression test in `tests/test_tree_vps.py`.
- `pytest.ini` registers the `vps_integration` marker; `--integration` flag works.
- Plan 01-01's SSH plumbing is verified end-to-end: walking the live VPS via the dashboard's walker now WORKS, closing out the M2 Phase 1 acceptance.
- The next phase (`Phase 2 — invisible-server systemd unit + nginx vhost`) can rely on `srv982719` SSH being solid.
</success_criteria>

<output>
Create `.planning/workstreams/vps-connection/phases/INV-01-ssh-controlmaster-invisible-toml-host/01-02-SUMMARY.md` when done. Record:
- Files created / modified with diff stats.
- Test counts: hermetic test count, live test count, pass/skip/fail breakdown.
- Which of Step 2A / 2B / 2C from Task 3 applied. If 2B: cite the failing test, the failing assertion, the patched lines, and the regression test added.
- A paste of the FULL output of `pytest tests/test_tree_vps_live.py --integration -v` (so the SUMMARY contains evidence the live VPS was actually reached).
- A paste of the `git diff --shortstat lib/api/tree_vps.py` showing the size of any change (likely zero).
- Confirmation that `ssh srv982719 echo ok` still works after the test run.
- Any deviations from this plan and why.
</output>
